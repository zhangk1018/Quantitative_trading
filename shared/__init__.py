"""
shared - 量化系统共享模块

【设计目标】
确保前台（frontend/）和后台（backend/）共用同一套：
1. 数据格式契约 (Pydantic schemas)
2. 常量定义 (枚举、字段白名单)
3. 配置加载 (与具体业务解耦的全局配置)

【迁移原则】
- 后台/前台共用的定义放这里
- 任何只被单一端使用的，不要放这里
- 修改这里会影响整个系统，需要慎重

【目录结构】
    shared/
    ├── __init__.py
    ├── schemas.py      # Pydantic v2 数据模型（数据契约）
    ├── constants.py    # 枚举/字段白名单/常量
    └── utils.py        # 前后端共用的工具函数
"""
