# 量量日报 2026-09-08

## 后台负责模块
数据管道、ETL、数据库、监控看板

## 今日完成

### 阶段2：数据下载
- **港/美股基本面同步补全**：新建 `sync_hk_basic.py`（AkShare 百度估值：总市值 + 市盈率 TTM，2802 只）、`sync_us_basic.py`（AkShare 新浪：市值 + PE，211 只），均带 `_load_dotenv()` 自动加载数据库连接、多头幂等写入 `stock_daily_basic` 分区表。
  - 港股补全到 **2026-09-07**：写入 2784 条，市值/PE 覆盖 100%。
  - 美股补全到 **2026-09-04**：写入 209 条，市值/PE 覆盖 100%（替换网易旧 09-02 数据）。
- **交易信号 + 宽表重算**：港股（信号 1566 条、宽表 2202 条到 09-07，PE 纳入 1267/2202）、美股（信号 130 条、宽表 205 条到 09-04，PE 纳入 195/205）。修复宽表在基本面落库前执行导致 PE=0 的问题，重跑后正确纳入。

### 阶段5：监控看板（P1 修复）
- **监控覆盖率口径误报修复**（`monitor.py`）：交易信号为事件驱动型（仅指标触发生成，非每日每只必有），原逻辑对港/美股用「行情股票数」当覆盖基准导致恒定误报（港股 1566/2202=71.1%）。修正为：`signal_precompute` 不分市场统一用固定阈值（100）；其余任务保留 A 股固定阈值 + 港/美股按该市场行情动态基准。
  - 修复后判定：技术指标 97.5%（原误报 53.7%）、K线形态 97.5%、交易信号 success。
- **美股监控期望交易日**：引入 `pandas_market_calendars>=5.4.0`（SSE/HKEX/XNYS 统一交易日历，失败自动降级 US_HOLIDAYS_2026 + 周末兜底，不阻塞启动）。

### 阶段7：定时调度（配置）
- **周六基本面补全迁移到 launchd 统一管理**：新增 `com.quant.hk_basic_sync.saturday.plist`（周六 12:00，百度估值）/ `com.quant.us_basic_sync.saturday.plist`（周六 13:00，新浪），纳入 `load_launchd_plists.sh` 复制与加载；删除此前用 Schedule 工具创建的临时定时任务。
- **港股 ETL 触发时间 17:00 → 21:20**（周一~五）：新浪港股数据源实测 21:14 才更新到位，调整后避免 17:00 跑了拉空/按旧日计算。`com.quant.hk_job.plist` 与注释同步。
- **美股任务链恢复基本面步骤**（`us_job_runner.py`）：将「基本面」加回 STEPS（此前实现为独立周六/手动，每日链回归含基本面）。
- **港股任务链基本面低频接入**（`hk_job_runner.py`）：新增 `_build_steps()` + `HKJOB_RUN_BASIC=1` 环境开关，基本面（百度逐只约 1 小时）默认不参与每日串行链，需低频/手动补全时再插入（不拖慢每日 21:20 调度）。

## 数据库变更
- 无结构迁移；仅数据写入 `stock_daily_basic`（market=hk/us 最新交易日）。

## 配置变更
- `scripts/launchctl/com.quant.hk_job.plist`：触发 17:00 → 21:20。
- `scripts/launchctl/com.quant.hk_basic_sync.saturday.plist`（新增）、`com.quant.us_basic_sync.saturday.plist`（新增）。
- `scripts/load_launchd_plists.sh`：加载「周六基本面补全」分组 + cleanup label。
- `backend/requirements.txt`：新增 `pandas_market_calendars>=5.4.0`（注释说明+降级）。
- 文档 `.trae/rules/ETL_PIPELINE.md`：港股调度 21:20、周六基本面补全小节（.trae 不入库）。

## 明日计划
- **协作单 32.0 遗留 P1**：在 `import_hk_daily.py` 导入层加「Adj Close 缺失 / ≤ Close×1.01 剔除回退 + 告警」复权有效性拦截（方舟 09-07 已清存量，需防复发增量防护）。
- 核验周六基本面补全任务随 launchd 正常首跑（下周六 09-12 12:00/13:00）。
- 监控看板页面确认港股三项（技术指标/形态/信号）恢复正常。