# 量量日报 2026-09-04

## 后台负责模块
数据管道、ETL、数据库迁移

## 今日完成

### 阶段1：前置检查
- **周/月K线覆盖率统计修复**（pipeline_health_check.py）：新增 `_period_daily_count`，覆盖率分母改为周期内实际有日线数据的股票数（此前用全市场总数导致 58% 虚低误报），周/月K线 58%→100%。

### 阶段2：数据下载
- **港股/美股日线下载限流 + 断点续传**（market_download_common.py 新增 + V013 迁移）：`rate_limit_sleep` 批间随机休眠 2.0~2.4s 统一控制 25~30 个/分钟；`etl_control` 新增 `last_processed_code` 游标列，每处理一只即回写，中断后断点续传；仅成功写入才回写 `last_sync_date`。
- **港股/美股新增 `--start-date` 区间下载参数**（import_hk_daily / import_us_daily `--init --start-date YYYY-MM-DD`，按 [起始日, 今天] 拉取）。
- **港股行情下载空跑排查与修复**（P0）：根因=新浪数据源 9/4 数据延迟未更新（抽样 10 只最新仅 9/3）而增量终点硬编码今天→整批「拉取为空」空跑。修复：新增 `_probe_src_latest` 增量前探测数据源最新日期，`run_incremental` 动态调整窗口终点、`start > end` 直接跳过（import_hk_daily / import_us_daily 对称修复 + 新增 6 单测，26 个相关测试全过）。已停空跑任务并清空断点游标，`last_sync_date` 保持 9/3 待数据源更新后重跑补数。
- **移除雅虎财经 yfinance 依赖**：删除 sync_hk_basic / sync_us_basic / sync_fx / check_index_integrity 四脚本；yahoo.py 精简保留 MarketConfig / normalize_code；港/美股列表同步改 AkShare（新浪）；卸载 venv 中 yfinance 包。
- **停止 hk_basic_sync 告警进程**（Yahoo 限流持续输出「基本面为空」告警，任务链已移除基本面步骤）。

### 阶段3：数据计算
- **周/月K聚合补 market 字段**（compute_bar_aggregation.py）：INSERT 用 `MIN(q.market)` 聚合 + ON CONFLICT 更新 market，修复港/美股聚合被标记为 cn；回算 2025+ 周/月K补全港/美股数据。

### 阶段4：数据导出
- 重跑导出 latest_quotes.parquet / latest_quotes_us.parquet。

### 其他
- **监控看板多项修复**（monitor.py / monitor.html）：动态基准按 market 过滤（A股排除 `.HK` 港股）、`latest_date` 按 market 过滤（修复港股日线清洗恒显 running）、`get_markets` 全表聚合超时修复、running 状态 4 小时僵死检测降级「疑似中断」；数据完整性总览移除「真正缺失」冗余列、提示文案明确=已合并+近期停牌。
- **监控三页签验证**：港股日线清洗状态修正为 failed 疑似中断、美股 08:30 自动任务全链路 success。

## 数据库变更
- V013_add_etl_control_resume.sql：`etl_control` 新增 `last_processed_code` 断点续传游标列。

## 配置变更
- 港股/美股限流节拍统一 25~30 个/分钟（MarketConfig `batch_interval_min_sleep=2.0` / `max_sleep=2.4`，yahoo.py）。

## 明日计划
- 待新浪更新 9/4 港股数据后重跑 `import_hk_daily --incremental` 补数（`etl_control.last_sync_date` 仍为 9/3）。
- 观察港股/美股数据源更新时刻，评估是否需要调整调度时间或补充重试机制。
- 30.0 协作单推进状态确认（V6 监控/定时已就绪，港/美股数据真实状态跟踪）。
