# 量量日报 2026-09-13

## 后台负责模块
数据管道、ETL、数据库迁移、快照历史服务、监控看板

## 今日完成

### 协作单 35.0：回测买入条件与选股视图口径不一致（P1，VERIFY 待方舟前端联调）
- **根因确认**：选股视图信任后端 ETL 预计算的最新一日宽表（/api/stocks/），回测退化为前端拿 OHLCV 重算技术指标+自编指标，导致口径不一致、基本面条件被剥离失效、自编指标评分日期索引错位
- **后端新端点 `GET /api/snapshot/history`**（snapshot_service.get_history_snapshots + snapshot.py 路由 + schemas 新增 SnapshotHistoryStock/SnapshotHistoryData）：
  - 按 `codes`+日期区间返回 `stock_daily_snapshot`（含 `stock_daily_basic` 的 pe/pe_ttm/pb/换手率/市值）全历史预计算字段，**66 字段白名单**（价格 12 + 技术 17 + 估值 10 + net_mf_amount + K线形态 5 + 技术 pattern 14 + 突破/连续/状态 7），与选股 /api/stocks/ 同一宽表同口径
  - codes 与 /api/stocks/ 的 stock_code 同格式（cn 6位 / hk 4位宽 `0700.HK` / us ticker）；`fields` 白名单裁剪；服务端游标流式读取
  - 规模保护：行数上限 60000（超出 400 提示缩小范围）、codes ≤2000、区间 ≤500 天
- **验证**：单测 14 例全过（tests/test_snapshot_history.py：参数校验 8 + fields 裁剪 2 + DB 集成 4）；随机抽样 5 股 × 66 字段与 DB 对账 mismatch=0；cn/hk/us 三市场实测正常
- **性能**（200 只 × 196 交易日）：全字段 11.4s / 48.8MB；fields 裁剪 24 列 6.7s / 16.3MB（已建议前端 fields 裁剪 + IndexedDB 缓存）
- **hk/us 宽表历史回填完成**：`daily_snapshot_sync.py --market us/hk --start-date 2025-07-30`——hk 2025-07-30→2026-09-11（277 交易日/最新 2681 只）、us 2025-07-30→2026-09-11（282 交易日/最新 205 只），与 cn 最新 09-11 三市场对齐；注意 hk/us 基本面历史仅 09-01 起有值、cn 2025-12 前部分指标列为 NULL（指标历史上线节奏，属数据现实）
- 35.0 状态 ASSIGNED→VERIFY，协作单处理记录 + topics.md 已同步，待方舟前端联调后 CLOSED

### 监控看板判据优化（存量补提交）
- `monitor.py`：任务状态判据从「今日是否执行」放宽为「最近期望交易日以来是否执行」（避免复权因子增量等 task 与港/美股滞后市场周末误报）；港/美股 task_run_log 按 `{market}:{label}` 前缀查询（防误用 A 股同 key 记录）；周K/月K 加入 MARKET_CHAIN 覆盖评估
- `sync_hk_basic.py`：新增 `_write_task_log`，周六港股基本面任务自动写 task_run_log（`hk:基本面`，stage=8），看板港股页签「基本面」自动显示成功，替代手动插记录

## 数据库变更
- hk/us 宽表历史回填（数据回填，无 DDL）：hk 607,080 行 / us 57,629 行，铺至 2025-07-30

## 配置变更
- 无

## 明日计划
- 跟进 35.0：方舟前端联调结果（engine 改用 /history 逐日判定 + 自编指标索引对齐），验证 K 例「09-01 选出 A → 回测进候选（T+1 成交）」
- 跟进 34.1：方舟复核 RSI 信号密度后 CLOSED
- 响应 36.0（方舟前端单）：若需后端透传自编指标算子/阈值序列化，配合确认
