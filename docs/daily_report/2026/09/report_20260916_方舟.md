# 方舟日报 2026-09-16

## 前台负责模块
回测分析、股票选择（级联下拉）、导入导出、自编指标/卖出策略

## 今日完成

### 1. 回测分析股票名称反查优化（P1，前端）
- 根因：回测分析的「股票代码/名称」级联下拉，来自自选股分组的股票名称依赖 `watchlistNames` 反查，原实现单次请求不传 market，港股/美股跨市场查不到名称
- `BacktestConfigPanel.tsx`：`watchlistNames` 加载改为**按市场分桶**并行请求（`inferMarketKey` 分 cn/hk/us）；下拉 label 由 `watchlistNames[code] || code` 改为 `?? ''`（查不到不再把代码冒充名称）；整组回测 `stockName` 同步用 `?? ''`
- `tradeExport.ts`：`resolveStockNames` 同样改为按市场分桶反查，确保导出 TXT/CSV 时港股/美股股票能拿到真实名称

### 2. 股票名称 Safari 缓存问题排查（P1，已定位非代码问题）
- 现象：内build置浏览器/Chrome 正常、Safari 异常（自选股分组只显示代码）
- 排查结论：后端 `/api/stocks/` 对 34 只 code 实测全部返回正确中文名，前端反查逻辑、vite 服务源码全部正确；确认是 **Safari 对 vite dev server 的 JS 强缓存**导致加载旧版前端
- 验证：Safari 无痕窗口 + 清理网站数据后显示正常，**无需改动代码**

### 3. 导入/导出功能升级（P0，上午）
- `ImportExportButtons.tsx`：导出升级为**选择 Modal + Checkbox 列表**（支持勾选部分指标后导出，而非全量）
- 新增 `SellImportExportButtons.tsx` + `SellStrategyModal.tsx`：卖出策略的导入/导出选择弹窗
- `customIndicatorStorage.ts`：`exportCustomIndicators` / `exportCustomSellStrategies` 增加 ids 过滤参数
- `SellStrategyManager.tsx`：卖出策略管理区接入导入导出能力
- `KLineChart.tsx`：关联渲染优化

## 变更文件
- `frontend/src/features/backtest/BacktestConfigPanel.tsx`（名称反查按市场分桶，+56 行）
- `frontend/src/features/backtest/utils/tradeExport.ts`（resolveStockNames 按市场分桶）
- `frontend/src/features/stock-picker/components/ImportExportButtons.tsx`（导出选择 Modal，+136 行）
- `frontend/src/features/config/components/SellStrategyModal.tsx`（新增，536 行）
- `frontend/src/features/config/components/SellImportExportButtons.tsx`（新增，366 行）
- `frontend/src/features/config/components/SellStrategyManager.tsx`（接入导入导出，+302 行）
- `frontend/src/features/stock-picker/utils/customIndicatorStorage.ts`（导出 ids 过滤）
- `frontend/src/features/backtest/utils/customSellStrategyStorage.ts`（卖出策略存储增强，+190 行）
- `frontend/src/features/stock-picker/components/KLineChart.tsx`（渲染优化）

## 明日计划
- 跟进控制台 IndexedDB `backtestStorage` object store 未初始化告警
- 关注财务指标库 `stock_fundamental_pit` 空表（待量量评估接入 Tushare 财务数据）