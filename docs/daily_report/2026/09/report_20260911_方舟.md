# 方舟日报 2026-09-11

## 前台负责模块
回测分析、条件检测器、交易台账

## 今日完成

### 1. 交易室「交易计划」表单功能增强（P1）
- 止损价随入场价联动：抽取 `syncStopLoss(entry)`，入场价失焦时止损价始终自动更新为 `入场价 × 0.95`（清除此前“仅止损价为空才填”的拦截），入场价改动后止损价随之变动
- 选中标的代码自动带出：标的搜索由单源 `searchStocks` 切换为跨市场 `searchStocksAll`（cn/hk/us，其返回携带前收盘价）；选中标的时 `入场价` 缺省 = 标的前收盘价（`pre_close`，缺失时退化用最新 `close`），并同时带出标的名称、联动止损价
- 统一同一文件内 ABC 分类编辑器的标的搜索逻辑（同样走跨市场，选中仅带出名称）
- `TradingPlanEditor.tsx` 清理不再使用的 `searchStocks`/`buildStockOptions`/`pickStockName` 导入

### 2. 前端控制台警告治理（antd，P2）
- `Collapse`：`destroyInactivePanel` → `destroyOnHidden`
- `Modal`：`destroyOnClose` → `destroyOnHidden`（全量替换）
- `Spin`：独立 `tip` 用法改为嵌套模式（`<Spin><div/span>…</div></Spin>`），修正“tip 仅 nest/fullscreen 生效”告警
- `useForm` 未绑定 Form 告警：为 6 个含表单的 `Modal` 补 `forceRender`，确保 Form 初始即挂载（TradingRecordForm/CycleOverview/ExitSlipModal/EquityCurve/ActModule/SaveStrategyModal）

## 变更文件（frontend，21 个）
- `features/pdca/components/TradingPlanEditor.tsx`（+104/-）— 止损/入场价联动、跨市场搜索
- `features/pdca/constants.ts`（+10）— 放弃条件挂钩模板（ABORT_CONDITION_BY_TYPE）
- `features/stock-detail/api.ts`（+1）— StockSearchItem 补 `pre_close` 类型
- 警告治理（19 个，各 1~5 行）：-backtest/index /watchlist/index/WatchlistSearchModal

## 明日计划
- 浏览器自测交易计划：输入代码 → 自动带出前收盘价入场价 + 0.95 止损价 → 改入场价止损价联动
- 确认交易室美股中文名缺口是否开协作单转量量