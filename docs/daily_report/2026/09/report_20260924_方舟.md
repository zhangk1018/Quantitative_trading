# 方舟日报 2026-09-24

## 前台负责模块
账号体系（登录/注册/改密/管理员页）、路由守卫、顶栏、自选股、回测分析、选股视图

## 今日完成

### 1. 协作单 42.0 前台部分：单密钥门禁 → 用户名+密码多账号体系（P1）
- **认证基础层**：新增 `features/auth/api.ts`（11 接口客户端 + `parseAuthError` 归一 401/403/409/429/503 业务 code）、`session.ts`（会话失效统一捕获：axios 响应拦截 + `window.fetch` 包装，401 `unauthenticated`/`disabled` → 清态踢下线跳登录）、`AuthContext.tsx`（`/auth/me` 单次探测 + 内存缓存、`/auth/config` 注册开关、门禁未启用放行、503 → 可重试错误态）
- **页面**：`Login.tsx` 改用户名+密码 + 按响应 code 区分提示（密码错/已禁用/服务不可用）+ 注册入口显隐；新增 `Register.tsx`（开关关闭显示提示；保留名/长度/一致性校验）、`ChangePassword.tsx`（旧密码校验，成功后清会话回登录页）、`UsersAdmin.tsx`（用户列表 + 建号/重置密码/禁用启用/改角色 + 确认弹窗与结果反馈 + 防自锁）
- **框架改造**：`App.tsx` AuthProvider 挂在 Router 之上（登录态跨路由复用）、`router.tsx` 守卫改用缓存登录态（401 跳登录、新增 `/register`、`/change-password`、`/users` 由 AdminGuard 按 role 控制）、`AppLayout.tsx` 顶栏当前用户 + role 标签 + 下拉（改密/用户管理(admin)/退出登录）
- **自选股按账号隔离**：去硬编码 `user_id=default`；localStorage 存储键命名空间化为 `watchlist:<username>`，多账号改造前的老全局键由管理员首次登录承接一次并删除，其它账号不承接（修复了联调中实测到的 A/B 串号）

### 2. 协作单 42.0 后台验收 + 缺陷闭环（方舟作为验证方）（P1）
- **后台验收 1~6 全过**：V014 两表 + bootstrap（DB 核验）、登录签发 sub+ver / 错密码 401 `bad_credentials` / 禁用 403 `disabled` / 无 Cookie 401 `unauthenticated`、改密与禁用后旧 token 立即失效且两设备互不影响、重名 409 与 DB 故障 503、自选股 CRUD 按账号隔离、独立复跑 `test_auth.py`+`test_watchlist_isolation.py` 26 例
- **浏览器端到端（前台验收 1~6）**：登录/顶栏+role/守卫缓存（4 次导航仅 1 次 `/auth/me`）/改密/管理员页（建号、409、重置、改角色、防自锁）/自选股 A/B 隔离/禁用踢人（401 `disabled` → 自动跳登录）
- **发现后台 P1 缺陷并推动闭环**：`POST /api/auth/logout`（`logout-all` 同）未下发清 Cookie 的 `Set-Cookie` 头 → 登出后按 F5 回到应用内；定位根因（`clear_session_cookie` 加在 FastAPI 注入的 Response 上，却 `return Response(204)` 新建对象致 headers 被丢弃）并给出修法 → 提单置 REOPENED
- **修复后复核通过（VERIFY→CLOSED）**：代码审查 + 独立复跑后台单测 **31 passed** + 无凭据 live 复核 **17/17**（`logout` 204 且 `Set-Cookie: access_token=""; Max-Age=0`、`logout-all` 后旧 token 401、自禁用/自降级 400 `cannot_modify_self`、业务路由 200）+ 浏览器端到端（登出 → 回 `/login` → **F5 仍停留登录页**、`/auth/me` 401）

### 3. 测试与回归（P1）
- 新增 `frontend/tests/auth/*` 8 个文件、**54 例全过**（authApi/session/AuthContext/Login/Register/ChangePassword/UsersAdmin/自选股隔离）；`tsc -b` 0 错误
- 全量前端回归对比 HEAD 基线（`git worktree` 建基线跑全量）：既有失败 19 例**逐项一致**、本次改动**零新增失败**（另 1 例 Monaco 时序类偶发单文件复跑通过）

### 4. 协作单看板治理（P2）
- 42.0 关闭后按约定归档；同时把遗留的 39.0/40.0/41.0（均 CLOSED）一并移入 `docs/协作单_归档.md`，`docs/协作单.md` 只保留流程说明（当前无进行中工单）

## 变更文件
- 新增（前台）：`src/features/auth/api.ts`(187) / `session.ts`(105) / `AuthContext.tsx`(159) / `Login.tsx`(137) / `Register.tsx`(161) / `ChangePassword.tsx`(129) / `UsersAdmin.tsx`(410)；`tests/auth/*`(8 文件 1032 行)
- 修改（前台）：`src/App.tsx`、`src/app/router.tsx`(+96/-…)、`src/app/layout/AppLayout.tsx`(+53)、`src/features/stock-detail/api.ts`(会话失效拦截)、`src/features/watchlist/api.ts`、`src/features/watchlist/store.tsx`(按账号隔离)、`src/features/auth/Login.tsx`
- 文档：`docs/协作单.md`(42.0 全记录 + 归档)、`.trae/topics.md`(跨会话通知/会话信息)、`docs/协作单_归档.md`（本地文件，`docs/*` 按仓库策略不入库）
- 随提交一并纳入的工作区变更（量量后台部分）：`backend/core/api/{passwords.py, bootstrap.py, security.py, dependencies.py, config.py, main.py, router/auth.py, router/watchlist.py}`、`backend/db/migrations/common/V014_add_users.sql`、`backend/tests/{test_auth.py, test_watchlist_isolation.py}`

## 明日计划
- 观察多用户账号体系实际上线使用（K 建号/分配角色）；提醒：`admin` 口令已在回归期间轮换，`.env` 的 `API_ADMIN_PASSWORD` 未同步（bootstrap 仅首建读取，不影响运行），建议对齐
- 抽空处理 5 个既有失败前端测试（CustomIndicatorManager、CustomIndicatorModal、ImportExportButtons、StrategyLoadingAndScreening、useBacktestWorker），恢复全量绿灯
- 跟进量量待办（`stock_fundamental_pit` 空表）对前台财务字段展示的影响（非前台职责，仅跟踪）