# 方舟日报 2026-09-19

## 前台负责模块
回测分析、选股视图、条件构建器、复盘报告（PDCA）

## 今日完成

### 1. 选股视图删除「因子打分配置」（P1）
- 移除侧边栏 FactorScoringConfig 组件与引用，删除组件文件
- 清理 indicatorConfig.ts 的 FACTOR_CONFIG / PanelKey 中 FACTOR 键
- 清理 ScreenerContext.tsx 的 factor 状态/reducer/SET_FACTOR_WEIGHT action
- 同步删除/调整 2 个测试文件中的因子引用（tsc 通过、58 例测试全过）

### 2. 「范围」面板缺省改为收起（P2）
- RangeSelector.tsx 由 defaultActiveKey 非受控展开改为受控模式（activeKey=panels.collapsed.range），
  缺省收起且折叠偏好可记忆，与其他面板行为一致

### 3. 复盘报告周期错位修复（P1）
- 复现：交易室→复盘报告，选 2026-09W1 显示 2026-08W4 内容
- 根因：CheckModule.tsx 切换周期时，新周期无复盘报告（fetchCheckReport 返回 null）
  未清空上一周期的 report/reportContent 状态 → 残留显示旧周期内容
- 修复：reportData/summaryData 为 null 时显式 setReport(null) + setReportContent('')（execSummary 同理）
- 浏览器验证：08W4→09W1 内容清空，切回 08W4 正常加载

### 4. 港股自编指标选股 0 只修复（P0）
- 复现：范围选港股 + 自编指标（港股核心量比 / close>ma5 测试指标）筛 0 只；
  技术指标（RSI超卖）可正常筛港股 → 定位自编指标管道
- 根因一：CustomIndicatorService 拉 OHLCV 调 /api/snapshot/all 未传 market，
  后端未传 market 默认按 cn 过滤 → 港股 OHLCV 全部返回空
- 根因二：computeMaxLookback 按公式最大数字切片（指标仅 5 日窗口），
  数据只留 5 根，指标 range(5,n) 需至少 6 根 → 全部算出 0 被过滤
- 修复：① fetchOhlcvBatch 按候选 codes 推断市场（inferMarketKey）传 market=hk/us；
  ② computeMaxLookback 加 +5 安全余量
- 验证：港股+close>ma5 → 1128 只；港股+港股核心量比 → 1187 只（修复前均 0）
- 排查期间重启后端服务（launchd，快照恢复 8074 只含港股）

### 5. 协作单 40.0 回测行情数据按日期范围（P1）
- 提单：回测行情仅覆盖最近 300 天（HISTORY_DAYS=300），需按前台日期范围准备 OHLCV
- 量量修复后验收：范围模式 API 417 天覆盖、前端传参（start_date=2024-10-24 含预热）、
  端到端回测交易次数 4/18 → 128 笔 → 复核通过 CLOSED

## 变更文件
- frontend/src/features/stock-picker/components/StockPickerSidebar.tsx（-2 行）
- frontend/src/features/stock-picker/components/FactorScoringConfig.tsx（删除，-71 行）
- frontend/src/features/stock-picker/config/indicatorConfig.ts（-14 行）
- frontend/src/features/stock-picker/context/ScreenerContext.tsx（-31 行）
- frontend/src/features/stock-picker/components/RangeSelector.tsx（+8 行）
- frontend/src/features/pdca/components/CheckModule.tsx（+6 行）
- frontend/src/features/stock-picker/services/CustomIndicatorService.ts（+7 行）
- frontend/src/features/stock-picker/hooks/useScreenerData.ts（+5 行）
- frontend/tests/config/indicatorConfig.test.ts（-42 行）
- frontend/tests/context/ScreenerContext.test.tsx（-18 行）
- docs/协作单.md（40.0 提单+验收）
- .trae/topics.md（跨会话提醒）

## 明日计划
- 跟进量量对后端快照服务加载一致性的确认（本次港股缺失由前端补传 market 解决）
- 视需要回归验证港股选股/回测全链路
