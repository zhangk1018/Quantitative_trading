# 方舟日报 2026-09-14

## 前台负责模块
回测分析、条件检测器、交易台账、股票筛选

## 今日完成

### 1. 协作单 37.0 / 38.0 指数历史数据复核闭环（P1）
- 独立复核量量补全的指数数据：沪深300（`000300`）/ 上证（`999999`）各 **1381 条**，覆盖 2021-01-04 ~ 2026-09-11，`market='index'` 隔离生效（快照/选股不含指数）。
- DB 核验 2024 年各 242 条 ✔；`GET /api/kline/000300.SH?start_date=2024-01-01` 返回 2024-01-04 收 3347.05。
- 37.0 复核通过 → CLOSED；38.0 复核通过 → CLOSED。
- 已 CLOSED 协作单（37.0/38.0）迁移至 `docs/协作单_归档.md`，主协作单体积 ~25KB → 18.4KB。

### 2. 回测预热窗口修复（协作单 36.0 复验，P1）
- `PREHEAT_DAYS` **60 → 250**（约一年交易日，覆盖 EMA50/Wilder ADX 无穷记忆指标收敛），修正 `KLINE_FETCH_LIMIT` 保持后端上限 **1000**（提 3000 触发 422 已回退）。
- 引擎对 `[firstValidIdx, startIdx)` 预热段命中信号显式写入 `signal_before_range` 诊断与 warning（不再静默丢弃），便于排查近起始日信号漏报。
- 复算：002508 2025-02-13 得分 8→ 命中 12 信号日、T+1 首买 = 2025-02-14 ✔；`tsc` 0 错误、backtestEngine 29/29 + 历史一致性 10/10。

### 3. 移除「指数 MA20 择时」设置（P1）
- 彻底移除策略回测与选股视图中的指数 MA20 择时：删除引擎 MA20 门控逻辑（`buildIndexMa20ByDate` 等）、`useIndexMA20Filter` 配置字段、UI 开关、`EnvironmentFilter` 组件及相关测试。
- 移除前为未提交工作区改动，拆除后文件状态与 git 基线一致。

### 4. 实现「每日最多建仓 N」Top N 截断（P2，防无差别全买）
- 回测引擎新增每日新增建仓上限 `maxNewPerRebalance`：开启时对当日目标池中未持有股票按 AST 得分降序排序，每日仅买前 N 只。
- 新增 `types.ts` 字段、`storage.ts` 默认值 0（不限制=旧行为）、`BacktestPanel.tsx` 输入框 + Tooltip 说明。
- 新增单测覆盖截断逻辑（`strategyBacktestEngine.test.ts` +55 行）。

### 5. ST / 退市整理股票名称排除工具（P3）
- 新增 `frontend/src/lib/stocks/exclusion.ts`：名称含 `ST` / `退` 的股票在选股候选池与回测买入候选池「计算自编指标之前」统一剔除（自编指标公式层拿不到股票名，仅 OHLCV）。
- 新增对应单测 `exclusion.test.ts`。

### 6. 修复「导出结果」CSV 下载测试失败（P2）
- 根因：`StockPickerBottomBar` 导出按钮是 antd Dropdown 触发器，默认 **hover** 触发，原测试仅 `click()` 导致菜单未展开、`onExport('csv')` 未执行。
- 修复：改用 `userEvent`，先 `waitFor` 按钮可用 → hover 展开下拉 → 点击「CSV 格式」菜单项。
- 验证：`StockPickerView.test.tsx` 9/9 通过，`tsc --noEmit` 零错误，jsdom "Not implemented: navigation" 为无害告警。

## 变更文件
- `frontend/src/features/backtest/constants.ts`（PREHEAT_DAYS 60→250 + KLINE_FETCH_LIMIT 约束注释）
- `frontend/src/features/backtest/backtestEngine.ts`（预热段信号显式化）
- `frontend/src/features/backtest/backtestTypes.ts`、`BacktestDiagnostics.tsx`（signal_before_range 支持）
- `frontend/src/features/strategy-backtest/engine.ts`（Top N 截断 / 移除 MA20 门控）
- `frontend/src/features/strategy-backtest/types.ts` / `storage.ts` / `BacktestPanel.tsx` / `DefaultsPanel.tsx` / `utils/dataLoader.ts`
- `frontend/src/features/stock-picker/hooks/useScreenerData.ts`
- `frontend/src/lib/stocks/exclusion.ts`（新增）、`frontend/tests/lib/stocks/exclusion.test.ts`（新增）
- `frontend/tests/components/StockPickerView.test.tsx`（导出测试修复）
- `frontend/tests/functions/strategyBacktestEngine.test.ts`（Top N 用例）
- `backend/collector/etl/sync_index_daily.py`（新增，量量遗留）、`backend/cron/daily_job_runner.py`（挂指数同步，量量遗留）
- `docs/协作单.md`、`docs/协作单_归档.md`、`.trae/topics.md`

## 明日计划
- 更新预算/持仓模块，适配 Top N 截断后的建仓节奏
- 待浏览器终验 36.0（002508 首买日 = 2025-02-14）后置 CLOSED
- 港美股基本面历史 NULL 前端判空兜底