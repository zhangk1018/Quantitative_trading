# 方舟日报 2026-09-03

## 前台负责模块

回测分析、条件检测器、交易台账【注：本次主体为 M6 港/美股前端与协作验证】

## 今日完成

### 1. M6 港/美股前端 · 市场工具层（P1）

- **T1** 选股器 `marketConfig` 解锁 hk/us + 补充 `currency`（CNY/HKD/USD）

- **T7** 新增 `formatStockCodeForDisplay`：港股补零至 **5 位**（K 批示 `9988.HK→09988.HK`）；恢复并补全 `detectMarketGroup` 港/美股识别

- **T8** 新增 `currency.ts`：统一币种格式化（¥ / HK$ / $），原始货币不换算，A股 CNY 不前缀（方案 §8 口径）

- **T9** PDCA 表单：`fetchDailyOHLC` 对港/美股带 `market` 参数（跨市场 OHLC 价格校验）；修复港股名称提取残留 bug（`9988.HK 阿里巴巴` 正则曾取到 `.HK 阿里巴巴`）；新增 `inferMarketKey`

- 单测：`stockCode.test.ts` / `currency.test.ts` 均通过

### 2. M6 选股器市场传导与隔离（P1）

- **T2** `buildScreeningParams`：非 cn（hk/us）传 `market` 参数，cn 不传（兼容旧后端）

- **T3** 市场隔离：`RangeSelector` 按市场隐藏「上市地」（hk/us 无板块）；`listed_board` 仅 cn 构造（防御式隔离）

- 实地勘察确认：条件构建器字段全为通用技术指标，无 is\_st/涨停 A股专属项泄漏，天然兼容港/美股

- 单测：`buildScreeningParams.test.ts` 全 31 例通过 + `tsc --noEmit` exit 0

### 3. 30.0 港/美股后端全链路验证（P1，交叉复核）

方舟侧 DB + API 双端验证 **V1-V5 全部通过**：

- **V1**：V008 迁移（market/currency/exchange/timezone + 分区级联 + 过滤索引 + fx\_rates），方案 B 唯一键口径（不含 market）

- **V2**：港股 ETL + 复权出口门槛（0700.HK 899 天 p99 偏差 0.0012%）

- **V3**：美股 ETL + AEP 除权复权连续性（adj 平滑、raw 除息跳空）

- **V4**：计算层 market 隔离 + parquet 分文件（cn/hk/us）+ fx\_rates/API

- **V5**：后端 API（market 参数 / display\_code 港股补零 / watchlist V012）门禁登录（时序演练）后 curl 复验

- **网络阻塞排查**：Yahoo 直连可用，系统代理 1082 拉断 Yahoo 分流（Shadowrocket 分流问题），已回执量量

### 4. 协作协调（P2）

- 美股列表口径：**K 确认先按 205 只（标普500）执行**，后续扩至 \~600，已回填协作单

- **31.0** K线除权日标注登记（后置待 K 确认，本期不实现）

- 晨检：数据管道健康（最新交易日 2026-09-02，5208 只；宽表/任务日志正常）

- 已向量量下发 V2/V3/V4/V5 推进清单与验证回执，30.0 已进入 V6（P0 监控 + M7 定时）

## 变更文件

- `frontend/src/features/stock-picker/config/marketConfig.ts`（解锁 hk/us + currency）

- `frontend/src/features/stock-picker/utils/screener.ts`（market 传导 + listed\_board 隔离）

- `frontend/src/features/stock-picker/components/RangeSelector.tsx`（按市场隐藏上市地）

- `frontend/src/features/stock-picker/context/ScreenerContext.tsx`（此前 T1 的无板块市场处理）

- `frontend/src/features/watchlist/utils/stock-utils.ts`（补零 5 位 + detectMarketGroup + inferMarketKey）

- `frontend/src/shared/utils/currency.ts`（新增）

- `frontend/src/features/pdca/services/record.ts`、`components/TradingRecordForm.tsx`（market 参数 + 名称提取修复）

- `frontend/tests/functions/stockCode.test.ts` / `currency.test.ts`（新增）、`buildScreeningParams.test.ts`（扩展）

- `docs/协作单.md`（30.0 V1-V5 验证回执、31.0 提单、美股 205 只确认）

## 明日计划

- 跟进量量 V6（P0 监控 + M7 定时任务），30.0 全部闭环后 CLOSED

- 推进 M6 前端剩余任务：T4（自选按市场分组）/ T5（K线详情市场传参 + 币种）/ T6（dataLoader 市场适配）

- M6 选股接口联调：量量 V5 API（market 参数 + display\_code）+ 门禁 token 已确认可用

- 将 M6 计划 / 30.0 定案同步写入协作单与跨会话提醒

