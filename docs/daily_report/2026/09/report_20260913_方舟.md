# 方舟日报 2026-09-13

## 前台负责模块
回测分析、策略回测、选股条件口径统一

## 今日完成

### 1. 协作单 35.0 后端历史快照字段出口验收（P1）
- 校验量量新增 `/api/snapshot/history`：单测 14 例全过；
- 独立服务层对账 cn(000001/600519)/hk(0700.HK)/us(AAPL/TSLA)，5 股 × 12 抽样字段与 `stock_daily_snapshot` 逐字段 mismatch=0、行数一致；
- HTTP 端到端：三市场混查一致、`trade_date` 升序、`market=cn` 正确剔除不符代码、超 500 天返回 400 守卫、`market_cap` 单位万元与前端 `MARKET_CAP_UNIT=10000` 换算一致；
- 前端联调单测 `backtestHistoryConsistency` 10/10（engine 按历史 Row 判定 / historyFieldMap / dataLoader 解析）。
- 遗留确认（写入单中）：hk/us 基本面历史值 09-01 前为 NULL，早于 09-01 且含基本面条件时需前端判空兜底。

### 2. 协作单 36.0 回测分析买入条件丢失阈值修复主修项实施（P1）
- **类型**：`backtestTypes.ts` 新增 `BacktestIndicatorOperator`（支持 `>`/`>=`/`<`/`<=`/`==`/`range`/`cross_up`/`cross_down`）与 `BacktestIndicatorThreshold`；`BacktestCustomCondition` 增加可选 `operator`/`threshold`，缺省退化 `score!==0` 兼容旧配置；
- **判定**：`backtestEngine.ts` 新增 `applyIndicatorThreshold()`，`computeBuySignals` 由 `score!==0`（把「10条件选股」0-10 得分任何非 0 当信号）改为按算子+阈值逐日判定（null/NaN 恒 false）；
- **透传**：`BacktestConfigPanel.tsx` 构造 buyCondition 携带 `indicator.operator`/`indicator.defaultThreshold`，使回测口径与选股视图一致；
- **诊断补齐**：`backtestTypes.ts` `DiagnosticEntry.event` 补 `buy_signal_holding_skip`，`BacktestDiagnostics.tsx` 新增该事件 UI 配置；
- 新增 6 例算子/阈值单测，`backtestEngine.test.ts` 29/29、历史一致性 10/10、`tsc` 0 错误；
- 单仓跳过子项（持有期信号不再静默丢弃）前面已在同单修复并入。
- 协作单 36.0 置 VERIFY，待 K 验收 002508「10条件选股」2025-01-01~ 买入日应与 12 个 `≥8` 信号日对应（T+1 成交）。

### 3. 回测口径统一改造（35.0 前端实施，承接上轮 F 节）
- `strategy-backtest` 7 核心文件（historyFieldMap 新增 + types/engine/dataLoader/StrategyBacktestView/filterTreeAdapter/worker）逐日判定口径统一切换到 `/api/snapshot/history` 后端预计算字段；
- 自编指标按日期键 zip 消除索引错位；不再剥离高风险基本面字段。

## 变更文件
- `frontend/src/features/backtest/backtestTypes.ts`（operator/threshold + 诊断类型）
- `frontend/src/features/backtest/backtestEngine.ts`（applyIndicatorThreshold + holding_skip）
- `frontend/src/features/backtest/BacktestConfigPanel.tsx`（operator/defaultThreshold 透传）
- `frontend/src/features/backtest/BacktestDiagnostics.tsx`（新诊断事件 UI）
- `frontend/tests/functions/backtestEngine.test.ts`（+6 例算子/阈值判定）
- `frontend/src/features/strategy-backtest/*`（historyFieldMap 新增、engine/types/dataLoader/StrategyBacktestView/filterTreeAdapter/worker 改造）
- `frontend/tests/functions/backtestHistoryConsistency.test.ts`（新增）
- `tests/run_10condition_screener.py`（独立复算程序，含区间/多股/CSV）
- `docs/协作单.md`、`.trae/topics.md`（工单更新）

## 明日计划
- K 复验 36.0 验收项（002508「10条件选股」买入日对账）+ 真实浏览器联调 35.0 K 例（09-01 选出 A → 回测 09-01 进候选 T+1 成交）后置 35.0/36.0 CLOSED；
- 港美股基本面历史 NULL 的前端判空兜底（承接 35.0 遗留确认）。