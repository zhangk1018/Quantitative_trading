# 量量日报 2026-09-07

## 后台负责模块
数据管道、ETL、数据库迁移、数据监控后端 API

## 今日完成

> 今日聚焦监控后端两条改造（33.0 按市场统计、33.1 美股误报修复），均为 `backend/core/api/router/monitor.py`（+前端 `monitor.html`）。港股脏数据清洗主体由方舟完成（见 32.0），量量侧导入层拦截仍为待办（见明日计划）。

### 阶段0：承接 32.0 港股清洗（状态说明）
- 港股脏数据（volume=0 占位、Adj Close 缺失/未复权、孤立未复权错价）的清洗、备份与全链路重算**由方舟完成**（见 topics 09-07 方舟记录：剔 216,498 行 + 27 行清算，库内未复权错价已清零）。
- **量量侧待办**：在 `import_hk_daily.py` 导入层增加「Adj Close 缺失或 ≤Close×1.01 即剔除回退+告警」拦截防复发，并对增量每日过检——**今日未实施，列入明日计划**。

### 阶段 X：监控看板按市场统计（33.0，后端+前端）
- 后端 `core/api/router/monitor.py`：
  - `_cached()` 缓存键支持模板（`data_summary:{market}`、`coverage_trend:{market}:{days}`），按参数生成市场专属缓存键，杜绝三市场串值。
  - `get_data_summary()` / `get_coverage_trend()` 新增 `market: str = Query("cn", pattern="^(cn|hk|us)$")`，行情/指标/信号/`stock_basic`（总/退市/ST）等 SQL 均按 `AND market=%s` 过滤，趋势基准改用该市场 `stock_basic` 数。
- 前端 `backend/static/monitor.html`：
  - `cacheData.summary/trend` 改为 `{cn,hk,us}` 三字典，`refreshAll()` 并行预加载三市场 summary/trend。
  - `renderCurrent()` 用当前 `marketTab` 对应数据渲染，新增 `dataSummaryTitle` 标注市场与口径；非 cn 页签隐藏「已退市/ST/已合并/近期停牌」A 股专属卡片。
- 本地真库自测：cn=5894/5207/99.58%、hk=2798/2205/82.34%、us=211/203/99.51%，三市场缓存相互独立。

### 阶段 X：美股任务链"三项待执行/数据未更新"误报修复（33.1，后端）
- 新增 `US_HOLIDAYS_2026`（纽交所休市日）+ `_get_last_us_trade_date()`：按美东时钟（ref−12h）跳过周末与美股假日，未收盘回退上一交易日；`_check_task_from_db()` 对 `market=='us'` 改用该函数，`cn/hk` 保持原逻辑。
- `stock_basic` 加入 `_MARKET_FILTER_TABLES`；股票列表改用 `stock_basic`（含 cn/hk/us，按市场过滤）并移除 `no_coverage`；「Parquet」步骤复用 `snapshot_sync` 宽表快照新鲜度。
- 验证：北京 09-07 18:17 时美股期望交易日=9/04；美股任务链 8 步全部 success（宽表/Parquet data_date=9/04、205 条）；港股无回归；`py_compile` 通过。

## 数据库变更
- 无新增迁移（本次仅清理存量错价数据，未改表结构）。

## 配置变更
- 无。

## 明日计划
- 待 K 在 Terminal.app 执行 `launchctl kickstart -k gui/$(id -u)/com.quant.backend` 重启后端，使 33.0/33.1 生效。
- 重启后请方舟复核协作单 33.0/33.1（美股期望日期、三市场统计、页签联动）。
- 跟进 32.0 备份表 `stock_quotes_hk_vol0_bak_20260907` 与港美股美股 2026 休市日维护（`US_HOLIDAYS_2026` 需按年度更新，后续可接入美股交易日历源）。