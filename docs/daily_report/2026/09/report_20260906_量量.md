# 量量日报 2026-09-06

## 后台负责模块
数据管道、ETL、数据库迁移

## 今日完成

### 阶段1：前置检查
- 无新增检查项。

### 阶段2：数据下载
- **港股/美股负后复权价问题定位（P0 紧急）**：前台港股选股报 `StockResponse` 校验失败（pre_close/open/close/high/low/amount 必须 >= 0）。根因定位：新浪 hfq 接口对**仙股**返回负后复权价（raw 原始价均为正），导入时未校验直接写入 `stock_quotes` 成交价列；美股锚点重建对 NEM 同样产生 45 行负价。影响范围：港股 272 只、60625 行（2000~2026-09-04）+ 43214 条负复权因子；美股 45 行 + 39 条负因子。

### 阶段3：数据计算
- **脚本防护（防再犯）**：`import_hk_daily.py` / `import_us_daily.py` 的 `clean_and_split` 新增负后复权价检测——检测到负 adj 价即回退为对应 raw 原始价、因子置 1.0，且置于 `detect_factor_dates` 之前避免负因子被误标除权日。单测通过（合成负 hfq 样例 → 全列转正、无假除权因子）。
- **存量数据修复（SQL）**：港股 60625 负价行回退 raw、56179 行 `pre_close` 按前一交易日修复后 close 重算、43214 条负因子删除；美股 45 行 + 43 行 pre_close + 39 条负因子同款修复。
- **港股全链路重算**：指标（全市场 2644 只）→ K线形态 `--latest` → 交易信号 → 宽表 `--latest`（2184 条/09-04）→ parquet 导出；NEM 指标单独重算。

### 阶段4：数据导出
- 重新导出 `latest_quotes_hk.parquet`（2184 行）/ `latest_quotes_us.parquet`。
- **验证通过**：hk parquet 2184 行逐行转换为 `StockResponse` **0 错误**、0 负值；`stock_quotes` / `stock_daily_snapshot` 港美股均 0 负值；parquet 加载器 mtime 热更新，前端刷新即生效无需重启后端。

### 其他
- **协作单 6.2-HK-SIGNALS 补数完成**（ASSIGNED→VERIFY）：9/4 日线入库后指标 2644 只/信号 2642 只对齐 09-04，宽表 2184 条 + parquet 已导出；抽查 0005.HK 行情/指标/信号全部对齐、9 月信号 4 条。
- **协作单 30.0-HKUS-MARKET V6 铺全量完成**（ASSIGNED→VERIFY）：HK/US 日线+指标+形态+信号+宽表+Parquet 全链路对齐 09-04（hk 2184 只 / us 205 只），A 股 cn 链路不受影响（snapshot 2,060,062 条至 09-04）。
- 清理 `temp/` 下今日修复脚本（负价修复 SQL ×2、选股验证脚本）。

## 数据库变更
- 无表结构变更；数据修复 SQL 仅更新数据（执行后已清理临时脚本）。

## 配置变更
- 无。

## 明日计划
- 待方舟对协作单 6.2 / 30.0 整单收尾验证后 CLOSED。
- 美股增量 6 只拉空（BRK-B 等，疑限流/停牌）下个交易日增量自然补回。
- 观察新浪 hfq 仙股负价是否复发，确认防护脚本生效。
