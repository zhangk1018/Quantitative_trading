# 方舟日报 2026-09-15

## 前台负责模块
回测分析、交易导出、系统设置自编指标

## 今日完成

### 1. 自编卖出策略功能（P0 新功能）
- 回测引擎新增 `custom` 卖出策略类型 + `BacktestCustomSellCondition`（纯信号协议），复用自编指标 Pyodide 执行管线，`pyodide-worker.js` 零改动
- 新增 `computeSellSignals` 全量预计算；主循环卖出分支在持仓状态消费自定义信号；脚本失败/长度不符自动回退内置「高点回落移动止损」并告警（非阻断）
- 3 个内置卖出策略行为完全不变，后端兼容旧配置（无 customSellStrategy 时回退内置）
- 新增存储层 `customSellStrategyStorage`（localStorage CRUD + 软删除 + 名称去重 + 公式校验）
- 引擎 mock 用例 4 例（触发/不触发/失败回退/长度不符）+ 存储测试 8 例，全部通过

### 2. 自编卖出策略管理迁移至系统设置（P0）
- 「管理自编卖出策略」入口从回测面板迁移到「系统设置 → 自编指标」，页面用 Segmented 分为「买入策略 / 卖出策略」
- 新建 `config/components/SellStrategyManager.tsx` 内嵌管理区块（列表 + 新建/编辑表单 + 删除）
- 回测面板移除管理弹窗，保留卖出策略选择下拉 + 迁移提示文案
- 新增 SellStrategyManager 组件测试 3 例

### 3. 交易明细导出（txt / csv）（P0）
- 新建共享模块 `backtest/utils/tradeExport.ts`（CSV 带 BOM、TXT 制表符分隔、带时间戳文件名）
- 单股交易明细导出：支持股票代码 + 股票名称列，导出前按代码反查真实名称（修复名称被代码替代 bug）
- 批量回测汇总新增「导出全部交易」按钮（TXT/CSV），合并所有股票交易并带代码/名称
- 回测测试 31 例通过，`tsc --noEmit` 零错误

### 4. 浏览器自测验证（P0）
- 本地验证《高胜率结构型卖出策略》脚本（600032 全量 1290 根 K 线）运行成功、信号长度一致
- 浏览器完整自测：系统设置 UI 分区正常 → 创建「结构型高胜率卖出v1」→ 回测 600032 产生 8 笔卖出（全部由自编策略触发）→ 无脚本错误/回退告警，全链路正确

### 5. 自编指标导入 ID 去重 bug 修复（P1）
- `existingIds` 去重未排除软删除记录，导致删除后导出→导入被误判重复
- 修复两处 `existingIds` 构造，与名称去重口径一致；新增回归测试

## 变更文件
- `frontend/src/features/backtest/`：backtestEngine/backtestTypes/BacktestView/BacktestConfigPanel/BacktestTradeLog/BacktestUniverseResult（改）、utils/customSellStrategyStorage.ts、utils/tradeExport.ts（新）
- `frontend/src/features/config/`：CustomIndicatorManager（改）、components/SellStrategyManager.tsx（新）
- `frontend/src/features/stock-picker/utils/customIndicatorStorage.ts`（改）
- `frontend/tests/backtest/`、`frontend/tests/config/`：新增引擎/存储/面板测试

## 明日计划
- 跟进自编卖出策略脚本在真实自选股上的回测效果验证
- 关注控制台 IndexedDB `backtestStorage` object store 初始化告警

## 备注
- 恢复/代码合并涉及：SellStrategyManager 迁移后命名一致（旧 BacktestSellStrategyManager 已删除）