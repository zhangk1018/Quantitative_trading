# 量量日报 2026-09-10

## 后台负责模块
数据管道、ETL、数据库迁移、信号服务、日志治理

## 今日完成

### 协作单 33.3：港股 factor 残留口径复核（P2，CLOSED）
- f=0 型 108 行/26 只（含 0021.HK）统一删除，备份 `temp_stock_adj_factor_fix_bak_20260910`
- 疑似类型 0206/3301 与行情 implied 一致属真实复权段不处理；0339/0612 错价回写对齐
- 全市场 factor(hk) f=0/孤儿/坏值/值不一致归零，`check_adj_factor_jumps` 除零消除，quotes 未动无需级联重算
- 方舟复核通过 CLOSED

### 协作单 31.0：K线除权日标注（后端部分，方舟前端联调后 CLOSED）
- **A股 factor_date 批量回填**：按 `detect_factor_dates` 口径（factor 相对变化>1%）回填 `stock_adj_factor`(cn) 31,488 行/4,703 只；000001/600519 分红节奏抽样验证正确
- **增量自动维护**：`sync_adj_factor.py` 的 `expand_factor_to_daily` 新增 factor_date 打标（含窗口边界 seed）+ `save_adj_factor` 持久化，4 边界场景单测通过
- **接口透出**：`kline_service.py` 新增 `_query_ex_dates` + `postgresql_storage.get_ex_dates`，`KLineResponse` 新增 `ex_dates` 字段；cn/hk/us 三市场验证正常
- 新增 `tests/test_adj_factor_factor_date.py`（6 用例）
- 后端重启后 `/api/kline/` 实测 000001 `['2026-06-12']`、600519 `['2026-06-26']` 正常返回

### 协作单 34.0：布林突破信号每日误报（P1，CLOSED）
- 根因：`get_kline_with_indicators` SQL 漏选 `boll_upper/boll_mid/boll_lower` 列 → 信号服务兜底转 0 → `close>0` 恒真每日误报
- 修复：SQL 补选 3 列 + `_generate_bollinger_signals` 加 boll 无效跳过守卫
- 实测 600036/000001/600519 布林 `>0.00` 误报归零，reason 全为真实值；方舟复核 CLOSED

### 协作单 34.1：RSI 信号高频（P3，VERIFY）
- 分析 4 只股票 RSI 分布：连续超买/超卖无合并导致 600036 年 98 条
- 方案：`_generate_rsi_signals` 改状态机合并——一段连续超买/超卖仅在退出阈值当天产 1 条拐点信号，保留 70/30 阈值（收紧至 75/25 边际收益小）
- 实测：600036 RSI 98→30、000001 98→28、600519 88→25；600036 信号日与走势相关性抽查合理
- 新增 `tests/test_signal_rsi_merge.py`（6 用例全过）
- 待方舟复核后 CLOSED

### 日志治理：ETL 明细/汇总分离（全项目）
- `logger.py` 新增 `setup_detail_and_summary_loggers` / `setup_detail_stdout_only`：明细仅 stdout（被 runner 捕获进 cron 明细），汇总写主日志文件
- 整改 7 个 ETL 脚本：fill_missing_data / import_daily_data / signal_precompute / sync_adj_factor / sync_daily_basic / import_hk_daily / import_us_daily——明细走 `logs/cron/{task}_{date}.log`，主日志（data_filler.log 等）只留汇总
- **hk/us runner 补 cron 明细落盘**：`_run_script` 原仅保留 stdout 最后 15 行，明细会丢失；新增 `_write_cron_detail` 将完整 stdout 写入 `logs/cron/{task}_{date}.log`
- pattern_precompute / daily_snapshot_sync 用 `configure_root_logging`（仅 stdout），本就正确，未改动
- 验证：py_compile + 冒烟导入 + 双 logger handler 断言 + dry-run 实测（明细不写主文件、汇总写入）均通过

## 数据库变更
- `stock_adj_factor`(cn) `factor_date` 批量回填 31,488 行/4,703 只（无表结构变更，属数据回填）
- 港股 factor(hk) 清理：DELETE 688 行 + UPDATE 4 行（33.3，备份表保留）

## 配置变更
- 无

## 明日计划
- 待方舟复核 34.1（RSI 信号）后置 CLOSED
- 关注晨检 WARN：港股缺失 96 只、09-08 hk 日线清洗僵死记录
- 待 K 确认排期项（如有新协作单）

## 备注
- 协作单 31.0/34.0/34.1 后端代码已随 d3510d3 提交（该 commit 为方舟前端提交，其中包含量量后端改动，commit message 未单列）；本次提交覆盖日志治理改造
- 今日提交 commit：日志治理（本次）
