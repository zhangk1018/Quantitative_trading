# 量量日报 2026-09-03

## 后台负责模块
数据管道、ETL、数据库迁移、港/美股后端全链路、系统监控

## 今日完成
按 ETL 阶段顺序组织

### 阶段1：前置检查
- 协作单 [30.0-HKUS-MARKET] V1（M1 表迁移 V008）落地并置 VERIFY（方舟复核通过后 ASSIGNED）：V008 加 market/currency/exchange/timezone 列、6 表加 market、分区非唯一过滤索引、fx_rates/etl_control 就位；构建公共迁移执行器 `backend/db/run_migrations.py`；pdca V001-V007 `--mark-applied` 归位

### 阶段2：数据下载
- 协作单 [30.0] V2 闭环（M2 Yahoo 适配器 + 复权 + 港股 ETL）：yahoo.py 适配器（MarketConfig/去前导零/分片429退避）、V009 迁移（raw_*/adj_* 8 列+factor_date）、adj_adjust.py 复权拆分、import_hk_daily/sync_hk_basic/sync_hk_stock_list；复权比对通过（腾讯700/AAPL vs 富途/同花顺 偏差≤0.0012%）
- 协作单 [30.0] V3 闭环（美股 ETL）：import_us_daily.py/sync_us_basic.py/sync_us_stock_list.py + V011（美股早期 1960-1989 分区补建）；美股核心池 `backend/config/us_core_universe.json`（标普500核心）落库 205 只；自测 AAPL/SPY 通过
- **P1 修复：A股日线导入误带港/美股**（本会话）：`stock_basic` 并入港/美股后，`import_daily_data.py` 选股未按 `market='cn'` 过滤，把 0001.HK 等 3000 只扫进 Baostock、完整度分母含港美股致 65.9% 假熔断乱触发 fallback。修复：`get_stock_list()`/完整度校验（existing_count、total_stocks）均加 `market='cn'`；`fill_missing_data.py` 补全条件追加 `sb.market='cn'`

### 阶段4：数据导出
- 阶段V4（M4 计算层复用 + M4.5 汇率）：5 计算脚本加 `--market`、normalize_db_code 市场感知、save_indicators/save_signals 补 market 列、loader 按市场加载、sync_fx.py 拉 HKDCNY/USDCNY + `/api/fx/rate`；cn 主链路回归无回归
- 阶段V5（M5 后端 API）：STOCK_CODE_PATTERN 放宽、get_market_loader/screener、stocks market 参数 + display_code（港股5位补零）、V012 watchlist market 列 + 唯一索引、_fix_listed_board 仅 cn；港/美股 K线、自选增删查实测通过
- 协作单 [30.0] V6（P0 监控 + M7 定时）：check_index_integrity/data_quality_monitor 扩展 market 维度 + 复权跳变告警、recompute_market、market_job_runner 拆分 hk/us_job_runner（任务状态写入 task_run_log，`hk:`/`us:` 前缀）、`/monitor/market-chain/?market=hk|us` 接口、港(17:00)/美(次日08:30 周二~六) 两个 plist

### 其他
- **美股定时任务时间**：`com.quant.us_job.plist` 由周一~五 22:00 调整为**周二~六 08:30**（届时美东前一交易日已收盘），港股 `com.quant.hk_job.plist` 保持周一~五 17:00
- **系统监控看板三页签**：`backend/static/monitor.html` 新增「沪深/港股/美股数据监控」三页签，展示各市场进程状态（task-run/market-chain 并行拉取缓存）
- 文档同步：`.trae/rules/ETL_PIPELINE.md` v1.6 补充港/美股任务链调度、分市场监控看板、三市场手动启动命令（.trae 目录 gitignore，不入库）
- 诊断：港股全量回填「拉取为空」根因 = Yahoo 限流(429)（非停牌/非代码格式）；实测 0080/0081/0700.HK 均可返回数据。已建议：init/incremental 循环加节流、429 空结果不算已同步、last_sync_date 全成功后才回写

## 数据库变更
- V008（加 market/currency/exchange/timezone，6 表 market，分区过滤索引，fx_rates/etl_control）
- V009（stock_adj_factor/stock_quotes 加 raw_*/adj_* 8 列 + factor_date）
- V011（美股早期 1960-1989 分区补建）
- V012（user_watchlist 加 market 列 + code 扩容 + 唯一索引）
- pdca V001-V007 `--mark-applied` 归位

## 配置变更
- `com.quant.hk_job.plist`（17:00 周一~五）、`com.quant.us_job.plist`（次日 08:30 周二~六）
- `backend/config/us_core_universe.json`（美股核心池 205 只）

## 明日计划
- 协作单 [30.0] V6 收尾置 VERIFY，30.0 整体 CLOSED（待 verifying 撤回后）
- 输出`import_hk_daily.py`限流节流修复供评审
- 与方舟对齐 M6 前端联调（门禁登录 token 已确认可用）

## 附注
- 工作区仍含方舟未提交文件（report_20260903_方舟.md 等）及数据产物（latest_quotes*.parquet/.bak），本次量量仅提交自身代码改动