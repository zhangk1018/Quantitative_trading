# 日报 - 2026-06-16

**负责人**：量量（后端 AI 工程师）  
**日期**：2026-06-16  
**天气**：☀️

---

## ✅ 今日完成

| 序号 | 任务描述 | 关联需求/Issue | 状态 |
|------|----------|----------------|------|
| 1 | **协作单 [6.6-TECHNICAL-API-20260616] 修复**：修复 MACD/RSI pattern 全为 0 问题。根因：字段映射错误（MACD→dif, MACD_SIGNAL→dea, MACD_HIST→macd）+ `self` 未定义（`_update_tech_patterns` 改为独立函数）+ RSI 缺少 12/24 窗口计算。重新计算全市场指标（5158/5531 成功），重新生成 parquet（5189 条，72 列） | [6.6] | ✅ VERIFY→CLOSED |
| 2 | **协作单 [6.8-FIELDS-DIFF-DEA-20260616] 修复**：修复 API 响应 `diff/dea/rsi_12/rsi_24` 返回 NULL。根因：`stock_daily_snapshot` 表缺少 4 个列，parquet 未导出。修复：ALTER TABLE 加列 + `daily_snapshot_sync.py` 更新 INSERT/SELECT/UPDATE + 重新导出 parquet（72→76 列） | [6.8] | ✅ NEW→VERIFY |
| 3 | **代码清理**：删除 `backend copy` 冗余目录；合并 3 个 shell 脚本（`start_prod.sh` + `start_service.sh` + `front_service.sh`）为统一 `start.sh`；修改系统仪表盘 URL 为 `http://localhost:8000/admin` | 代码治理 | ✅ |
| 4 | **文档更新**：新增 `docs/ETL_PIPELINE.md`（数据下载→清洗→计算→导出 parquet 全流程）；更新 `DATA_SCHEMA.md`（stock_daily_snapshot 新增 4 字段）；更新 `协作单.md`（6.8 状态 NEW→VERIFY） | 文档完善 | ✅ |
| 5 | **Git 提交推送**：3 次提交已推送 GitHub | 版本管理 | ✅ |

---

## 🔄 进行中

| 序号 | 任务描述 | 进度 | 预计完成时间 |
|------|----------|------|--------------|
| 1 | 协作单 [6.8] 待方舟验证 | 100% | 2026-06-17 |

---

## ⚠️ 问题与风险

| 序号 | 问题描述 | 影响范围 | 严重程度 | 解决方案/状态 |
|------|----------|----------|----------|--------------|
| 1 | **MACD 字段映射错误**：`compute_indicators_daily.py` 中 MACD 字段映射错误（MACD→dif, MACD_SIGNAL→dea, MACD_HIST→macd），导致 pattern 计算全为 0 | 技术指标计算 | 高 | ✅ 已修复字段映射 |
| 2 | **parquet 缺少 4 个字段**：`stock_daily_snapshot` 表缺少 `dif/dea/rsi_12/rsi_24` 列，导致 API 响应 NULL | API 字段透出 | 中 | ✅ 已 ALTER TABLE 加列并重新导出 parquet |
| 3 | **`daily_snapshot_sync.py` self 未定义**：`_update_tech_patterns` 作为类方法调用但实际是独立函数 | 宽表同步 | 高 | ✅ 已改为独立函数调用 |

---

## 📋 明日计划

| 序号 | 任务描述 | 优先级 |
|------|----------|--------|
| 1 | 确认方舟验证协作单 [6.8] 字段透出 | P0 |
| 2 | 前端 K 线图/详情页确认 diff/dea/rsi_12/rsi_24 显示正常 | P1 |
| 3 | 启动 P3.1 CustomIndicatorModal 开发（如 P2 评审通过） | P1 |

---

## 📊 Git 提交记录

| Commit | 描述 |
|--------|------|
| `2dba04d` | feat: fix API diff/dea/rsi_12/rsi_24 NULL fields, cleanup backend copy, update docs |
| `5f5adb7` | fix: MACD/RSI 指标字段映射修复 + pattern 计算逻辑优化 |
| `a963fc7` | feat: 技术指标 pattern API 支持 + 前端技术筛选模块 + 启动脚本合并 |

---

## 🔍 技术细节

### 协作单 [6.6] 修复要点

1. **字段映射修复**（`compute_indicators_daily.py`）：
   ```python
   # 修复前
   'MACD': DIF, 'MACD_SIGNAL': DEA, 'MACD_HIST': MACD_HIST
   # 修复后
   'dif': DIF, 'dea': DEA, 'macd': MACD_HIST
   ```

2. **RSI 多窗口计算**：新增 RSI 12/24 窗口计算，与 RSI 6 一起写入 `stock_indicators`

3. **pattern 计算独立化**：`_update_tech_patterns` 从类方法改为独立函数，避免 `self` 未定义错误

### 协作单 [6.8] 修复要点

1. **数据库加列**：
   ```sql
   ALTER TABLE stock_daily_snapshot ADD COLUMN IF NOT EXISTS dif DECIMAL(12,4);
   ALTER TABLE stock_daily_snapshot ADD COLUMN IF NOT EXISTS dea DECIMAL(12,4);
   ALTER TABLE stock_daily_snapshot ADD COLUMN IF NOT EXISTS rsi_12 DECIMAL(8,4);
   ALTER TABLE stock_daily_snapshot ADD COLUMN IF NOT EXISTS rsi_24 DECIMAL(8,4);
   ```

2. **parquet 列数**：72 → 76（新增 dif/dea/rsi_12/rsi_24）

3. **验证结果**：
   - 300005 探路者：diff=0.0458, dea=-0.0566, rsi_12=66.16, rsi_24=56.61 ✅
   - pattern 筛选：tech_macd=low_golden_cross 返回 145 条 ✅

---
