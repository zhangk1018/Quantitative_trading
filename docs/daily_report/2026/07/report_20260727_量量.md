# 量量日报 2026-07-27

## 一、今日完成

### 1. B1 分区裁剪优化 — 排期
- 编写 [B1 分区裁剪优化计划](file:///Users/zhangk/workspace/Quantitative_trading/docs/plans/B1_分区裁剪优化计划.md)，含 3 种方案（物化临时表 / 拆分窗口计算 / 覆盖索引）
- 计划 7/28 执行方案 A+B

### 2. 低优项批量修复（7 项全部完成）

| 编号 | 问题 | 文件 | 改动 |
|------|------|------|------|
| A3 | TushareDataSource 抛 NotImplementedError | tushare.py | 改 return pd.DataFrame() + logger.warning，避免调用方崩溃 |
| A4 | baostock 全局线程池未关闭 | baostock.py | 新增 atexit.register 安全关闭线程池 |
| C2 | 路径遍历风险 | config.py | APP_ENV 增加正则校验，非法值回退 dev |
| D3 | 事务管理混乱 | sync_stock_list_baostock.py | get_connection() 迁移到 database.py 的 get_raw_connection() 上下文管理器 |
| J1 | config.py 可变默认字典 | config.py | .copy() → copy.deepcopy() |
| J2 | rate_limiter 等待不响应中断 | rate_limiter.py | time.sleep() → threading.Event().wait() |
| J5 | 双数据源字段差异 | sync_stock_list_baostock.py | 已标准化（两个 fetch 函数返回统一 schema），无需改动 |

### 3. ETL 故障修复
- **问题**：监控系统报告"股票列表同步"和"增量导入行情"失败，exit code 1
- **根因**：
  1. `sync_stock_list_baostock.py`：sys.path 缺少 backend/ 目录 → ModuleNotFoundError
  2. `import_daily_data.py`：config.py 缺少 import copy → NameError
  3. baostock.py / tushare.py：RateLimiter 未导入 → NameError（预存 bug，传递导入被掩盖）
- **修复**：补全缺失的 import 和 sys.path，两个脚本均验证通过

## 二、风险与问题

- Tushare / Baostock 当前均不可用（Tushare 频率超限，Baostock 黑名单），属于运行时环境问题，非代码 bug
- 数据源恢复后脚本即可正常运行

## 三、明日计划

1. 执行 B1 分区裁剪优化（方案 A：物化 qdata 临时表）
2. 验证 EXPLAIN ANALYZE 结果，确认分区裁剪生效