# 方舟日报 2026-07-12

## 完成项
1. 关闭 TECH-DEBT-BUILD-20260712 工单（剩余构建错误已全部修复）
2. 修复 StockPickerView A11/A12 表格渲染测试（共 13 个测试全部通过）
   - 根因 1：`vi.spyOn` 只作用于测试文件的 namespace import，无法影响 `useScreenerData` 的 named import，改用 `vi.mock` 替代
   - 根因 2：`@tanstack/react-virtual` 在 jsdom 中 `getVirtualItems()` 返回空数组，mock 整个模块绕过 DOM 尺寸依赖
3. 条件构建器 UI 优化：数量显示从"X 个条件"改为纯数字 badge，与行情指标/技术指标/财务指标统一风格，并调整间距
4. 自选股页签增加滚动条：`min-h-screen` → `h-screen`，表头 sticky 固定，仅数据行滚动
5. 选股视图功能完整性排查：确认 12 项功能已实现，仅因子打分后端未接入（需开协作单）

## 待办
- 因子打分后端接入：需给量量开协作单，前端只需在 `buildScreeningParams` 中传递 `factor.weights`