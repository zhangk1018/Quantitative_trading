# 量量日报 — 2026-07-24

## 完成事项

### 1. ETL 管道维护（全流程执行至 2026-07-24）

- **数据下载**：增量导入完成，stock_quotes 最新日期 **2026-07-24**，5197 只股票
- **缺失数据补全**：Baostock 超时卡死（已杀死进程），仅 14 只新股需补全，跳过补全继续后续任务
- **基本面同步**：5526 条（Tushare Pro 主用，Baostock 不可用时跳过 pe_ttm 补全）
- **技术指标计算**：5190/5866 只，1,010,700 条记录
- **形态识别**：4190 只，7683 行（hammer/morning_star/engulfing 等）
- **交易信号生成**：4243 只，7688 条信号
- **宽表同步**：5197 条，5168 行 pattern 已更新
- **Parquet 导出**：5197 条 / 92 列，已更新

### 2. 监控看板图表修复（monitor.py）

- **问题**：指标计算、信号预计算等任务因覆盖率未达 100% 显示 `partial`，但实际数据正常（新股自然不足）
- **修复**：
  - 指标/信号任务使用 `COVERAGE_SUCCESS=95%` 阈值替代 100% 硬要求
  - 信号预计算使用 `TASK_MIN_COUNTS=100` 独立阈值，不用行情股票数作为基准
  - 监控看板 9/9 全部 `success`

### 3. 修复 `fill_missing_data` 卡死问题（永久方案）

- **根因**：Baostock API 调用在某些股票上无限挂起，全局线程池仅 2 个 worker，被僵尸线程占满后阻塞
- **双重修复**：
  - **baostock.py**：线程池 `max_workers 2→5`，降低池满概率
  - **fill_missing_data.py**：每只股票使用独立线程 + 120s 超时保护，超时自动跳过不阻塞

### 4. 更新文档（ETL_PIPELINE.md）

- 数据源优先级链按日线行情/基本面/股票列表三列分别说明，删除 Akshare/Tencent/Sina 引用
- 新增 `pattern_precompute` 独立章节（3.2 K线形态识别）
- 更新每日执行脚本推荐使用 `daily_job_runner.py` 一键执行

### 5. 定时任务恢复

- 清理废弃 plist 文件，刷新 3 个阶段 plist 加载脚本
- 已卸载旧版 `com.quant.daily_job.stage1/2`，加载脚本已就绪

## 数据质量

| 指标 | 数据 |
|------|------|
| stock_quotes 最新日期 | 2026-07-24, 5197 只 |
| stock_daily_snapshot 最新日期 | 2026-07-24, 5197 条 |
| 字段完整性 | 基本面字段 100%，pe 73%（亏损股无 pe 正常） |
| 任务执行日志 | 1 个失败（fill_missing_data 超时跳过），其余全部 success |

## 问题

- **Baostock 不稳定**：多次出现 API 调用挂起，虽已加入超时保护，但 pe_ttm 补全和 fill_missing_data 能力受限。后续考虑引入 pytdx 作为 fill_missing_data 的备用数据源

## 明日计划

- 监控 7/25（周六）无数据管道，系统状态正常
- 待 7/27（周一）收盘后，确认 launchd 定时任务自动触发 ETL 管道

---

**提交时间**：2026-07-24 19:50