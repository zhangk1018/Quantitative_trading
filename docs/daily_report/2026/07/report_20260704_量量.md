# 量量日报 — 2026-07-04

## 今日完成

### P0：启动脚本 → nc -z 端口检查
- [x] `scripts/start.sh`：改用 `nc -z` 检查端口存活，不等待数据加载完成，启动时间从 30s 超时失败降至 **< 5s**
- [x] 冷启动场景：检查进程存活即返回成功，不 timeout 强杀

### P0：Parquet 预序列化缓存
- [x] `backend/core/service/snapshot_service.py`：启动优先加载 Parquet 缓存（14MB），命中时加载耗时 **~1-2s**
- [x] `row_count_hash` 校验：覆盖数据回填/修正场景
- [x] HMAC 签名校验：防反序列化注入
- [x] 缓存文件自动生成于 `data/cache/`（ohlcv.parquet 14MB + snapshot.parquet 459KB）

### P1：服务端游标 + fetchmany 分批
- [x] 命名游标 `cursor(name="ohlcv_cursor")` 流式读取，替代 `fetchall()` 一次性拉取 101 万行
- [x] `fetchmany(20000)` + DataFrame 向量化分组，避免纯 Python 逐行循环
- [x] 3 次重试 + 退避机制

### P2：分段日志
- [x] `[_load_all]` 前缀的分段计时日志

### P3：异步后台加载 + 就绪检查
- [x] `ServiceNotReadyError` 异常
- [x] 后台线程异步加载数据，不阻塞 FastAPI 启动
- [x] `_ensure_ready()` + `wait_ready(timeout)`
- [x] `get_status()` 运维状态接口
- [x] `/api/snapshot/ready` 端点（返回 200 就绪 / 503 加载中）
- [x] 后端 `/health` 端点不依赖数据加载，始终可用

### 其他修改
- [x] `main.py` lifespan：移除 `get_all_snapshot()` 阻塞调用
- [x] SQL 参数化修复：`INTERVAL '%s days'` → `CAST(%s AS INTERVAL)`

### P1：K线弹窗 TradingView 集成（前端协助）
- [x] 回退"股票学习"功能代码（删除 stock-learning 目录、移除 /learn 路由、移除菜单项）
- [x] 双击股票行弹出 TradingView K线窗口，三段式全屏结构：顶部行情头栏 + 主图BOLL K线 + 副图MACD/成交量
- [x] TradingView CDN 国内访问问题：切换到 CloudFront 镜像 `d33t3vvu2t2yu5.cloudfront.net/tv.js`
- [x] 修复股票代码前缀错误：`SH:`/`SZ:`/`BJ:` → TradingView 标准 `SSE:`/`SZSE:`/`BSE:`（覆盖主板/创业板/科创板/北交所）
- [x] 修复 Modal 动画导致容器尺寸为0时 Widget 无法创建 iframe 的时序问题（300ms 延迟初始化）
- [x] 修复 `destroyOnClose` 废弃 API → `destroyOnHidden`
- [x] 移除重复的 Volume study（TradingView 蜡烛图自带成交量面板）
- [x] 增加 try-catch 错误处理和失败状态提示

## 性能数据

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| `start.sh` 返回"启动成功" | 30s 超时失败 | **< 5s** |
| `/api/snapshot/all` 首次请求 | 不可用 | **~5s**（含缓存加载） |
| `/api/snapshot/all` 后续请求 | 不可用 | **~2.3s**（内存缓存） |
| `/api/snapshot/incremental` | 不可用 | **~0.3s** |
| 缓存文件 | 无 | `ohlcv.parquet` 14MB + `snapshot.parquet` 459KB |

## 待解决问题的说明

- 已修复 `conn.autocommit = True` 导致命名游标不可用的问题（`can't use a named cursor outside of transactions`）
- 已修复 `pd.read_sql()` 不支持 `cursor` 参数的问题
- 已修复 `get_status()` 中 `_load_time` 未初始化的问题
- 启动后数据在后台线程加载，可通过 `/api/snapshot/ready` 查询就绪状态
- TradingView Widget 内部 iframe 加载 `s.tradingview.com/widgetembed/` 依赖外网，国内网络环境下首次加载需等待 CDN 资源，后续由浏览器缓存加速

## 明日计划

- 等待 K 验证后端启动修复效果
- 检查 Parquet 缓存是否在首次冷启动后正常生成
- 配合方舟验证K线弹窗交互体验，如有问题继续修复
