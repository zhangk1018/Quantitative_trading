# 量量 日报 2026-07-17

## 今日完成

### 1. Tushare 全量日线回填

- **背景**：对比测试 Tushare 和 Baostock 数据源可靠性，确定 Tushare 批量按日期提取效率更高
- **操作**：修改 `tushare.py` 批量接口新增 `ah_vol`/`ah_amount` 字段，修改 `import_daily_data.py` 和 `postgresql_storage.py` 支持新字段写入
- **执行**：创建 `temp/backfill_tushare_daily.py` 脚本，按日期循环拉取 2023-01-01 至 2026-07-17 共 856 个交易日数据
- **结果**：成功 839 天，空数据 16 天（非交易日），失败 1 天（2026-07-17 未收盘），共插入 4,271,775 条记录

### 2. 自编指标调试修复（3 次迭代）

| 问题 | 根因 | 修复 |
|------|------|------|
| 8条件投票筛选 5000 只通过 4000 只 | RSI 公式错误 + 波动率度量错误 + 默认阈值配置 | 修复 RSI 公式、改用 ATR 百分比波动率 |
| 8条件投票筛选结果全空 | `pyodide-worker.js` 参数顺序错误 + 未批量处理 | 修正参数顺序为 `(open, high, low, close, volume)`，遍历每只股票逐只计算 |
| 自编指标选股无股票入选 | `.toJs()` 对基本类型报错，所有结果置为 null | 新增 `toJsValue()` 智能判断返回值类型 |
| 筛选结果 4937/5197（95% 通过） | `meetsThreshold` 中未处理操作符默认返回 true | 未处理操作符明确返回 false 并打印警告 |
| 新公式测试 3/20 通过（15%） | 旧公式中 None 值直接比较报错 | 新公式使用 `np.array([x for x in ... if x is not None])` 过滤空值 |

**最终状态**：新公式（含 RSI(14) < 90、Momentum > 1%、Volatility < 6%、Volume > SMA20、ADX > 25、Price > EMA50/200、MACD > Signal）在 20 只股票测试中通过率 15%，无 Python 错误。

### 3. Baostock 连接重试优化

- **问题**：`_ensure_connected()` 中 3 次重试之间无延迟，Baostock 服务器短暂不可用时瞬间全部失败
- **修复**：重试间加入渐进延迟 5s → 10s → 15s，给服务器恢复时间
- **文件**：[baostock.py](file:///Users/zhangk/workspace/Quantitative_trading/backend/collector/datasource/baostock.py#L155-L162)

### 4. 今日数据管道状态

| 步骤 | 时间 | 状态 |
|------|------|------|
| pipeline_health_check | 16:05 | ✅ |
| stock_list_sync | 16:05 | ✅ 5,528 只 |
| daily_import | 17:30 | ✅ |
| adj_factor_sync | 17:31/17:49/18:06 | ❌ 失败×3（Baostock Broken pipe） |
| etl_pipeline 整体 | 18:06 | ❌ 在 adj_factor_sync 中断 |

**当前数据**：
- `stock_quotes`：2026-07-17（5,195 只）✅
- `stock_daily_snapshot`：2026-07-16（5,197 只）⚠️ 未更新
- `stock_indicators`：2026-07-16 ⚠️ 未更新
- `trade_signals`：2026-07-16 ⚠️ 未更新

## 问题与风险

- 今日 ETL 管道因 `adj_factor_sync` 失败中断，后续步骤（指标计算、信号生成、宽表同步、Parquet 导出）未执行
- 已修复 Baostock 重试无延迟问题，需明日验证
- `pe_ttm` 字段完整性 71.4%（持续性边缘问题，未恶化）

## 明日计划

- 补跑今日 ETL 管道剩余步骤（指标计算 → 信号生成 → 宽表同步 → Parquet 导出）
- 验证 Baostock 重试修复效果