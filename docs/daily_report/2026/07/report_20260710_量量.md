# 量量 日报 2026-07-10

## 今日完成

### 1. 修复 `daily_job_runner.py` stage 参数传入逻辑
- **问题**：`run_task_chain` 写入 `etl_pipeline` 整体状态时使用 `stage=0`，但数据库 `CHECK` 约束要求 `stage BETWEEN 1 AND 11`，导致写入失败退出码为 1
- **修复**：将 3 处 `stage=0` 统一改为 `stage=1`，新增 `PIPELINE_STAGE=0` 常量隔离语义
- **迁移**：创建 SQL 迁移脚本，将约束改为 `CHECK (stage >= 0 AND stage <= 11)`，已执行

### 2. 编写 `test_daily_job_runner.py` 测试套件
- 覆盖 13 个测试类、47 个测试用例，100% 通过
- 覆盖常量验证、工具函数、数据库查询、僵尸清理、任务日志、任务执行、任务链、文件锁
- 修复了 `TestRunTask` 框架的 `StopIteration` 异常和 Mock 异常类匹配问题

### 3. 监控看板问题排查
- **数据完整性**：排查 8 只缺失股票，确认 7 只正常停牌 + 1 只（德邦股份 603056）`delist_date` 未更新
- **复权因子同步**：排查 Baostock 连续 3 天（7/8-7/10）17:30 连接失败，导致 `stock_adj_factor` 无新数据
- **修复**：`sync_adj_factor.py` 在连接失败时 `return` 改为 `sys.exit(1)`，确保 runner 正确标记失败

### 4. 定时任务状态检查
- 确认 `com.quant.daily_job.stage1`（16:05）和 `stage2`（17:30）退出码 1 为旧代码遗留问题，下次触发自动修复

## 代码变更

| 文件 | 变更 |
|------|------|
| `backend/cron/daily_job_runner.py` | 修复 stage 参数 + 僵尸清理逻辑 |
| `backend/tests/test_daily_job_runner.py` | 新增 47 个测试用例 |
| `backend/collector/etl/sync_adj_factor.py` | 连接失败 `return` → `sys.exit(1)` |
| `backend/collector/etl/fix_missing_stocks.py` | 同上 |
| `backend/collector/db/sql/migrate_task_run_log_stage_check.sql` | 新增迁移脚本 |

## 问题与风险

- **Baostock 17:30 时段不可用**：已连续 3 天，可能是服务器负载/维护问题，需关注明日是否恢复
- **德邦股份 delist_date 未更新**：已给出修复 SQL，等待 K 确认执行

## 明日计划

- 关注 Baostock 恢复情况，确认复权因子同步自动修复
- 配合 K 审核测试用例后进入测试执行阶段