# 量量 日报 2026-07-13

## 今日完成

### 1. is_st 字段填充（数据修复）

- **背景**：`stock_daily_snapshot` 表 77,801 条记录的 `is_st` 字段全部为 NULL，影响前端 ST 股标识和涨跌幅限制判断
- **根因**：ETL 脚本 `daily_snapshot_sync.py` 的 SQL 中写死 `NULL::boolean AS is_st`
- **修复**：
  - `daily_snapshot_sync.py`：`NULL::boolean` → `CASE WHEN b.name LIKE '%ST%' THEN TRUE ELSE FALSE END`
  - `export_parquet.py`：修复导出路径为绝对路径（之前导出到 `backend/data/`，与 DataLoader 加载路径不一致）
- **执行结果**：
  - 数据库回填：`UPDATE 77801` 条
  - `is_st=true`：3,602 条 ST 股，`is_st=false`：126,483 条正常股，`NULL`：0
  - Parquet 缓存已重新生成（5,194 条，84 列）
  - API 验证通过：*ST九鼎 `is_st=True`，贵州茅台 `is_st=False`，无假阳性

### 2. 数据管道晨检

- 5 通过 / 3 警告 / 1 失败 / 0 异常
- 行情新鲜度 FAIL 属正常周末现象（7/10 周五 → 7/13 周日）
- 其余 3 项警告均为已知持续问题

## 问题与风险

- 无新增问题

## 明日计划

- 继续处理缺失数据排查（19 只活跃股最新交易日缺失数据）