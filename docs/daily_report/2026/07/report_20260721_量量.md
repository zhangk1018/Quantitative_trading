# 量量日报 — 2026-07-21

## 完成事项

### 1. 阶段合并（v9 → v10）
- **问题**：阶段3（18:15，复权因子+补全）耗时37分钟，阶段4（18:30）启动时文件锁冲突，导致阶段4每天被跳过。
- **修复**：将阶段3和阶段4合并为单一阶段3（18:15），共8个任务，线性执行，消除文件锁竞争。
- **影响文件**：
  - `backend/cron/daily_job_runner.py` — 合并STAGE3_TASKS/STAGE4_TASKS，移除STAGE_ADJ_FACTOR常量，更新全量调度逻辑
  - `scripts/launchctl/com.quant.daily_job_runner.stage4.plist` — 已迁移至 `temp/old_plists/launchctl_disabled/`
- **附带修复**：`get_last_batch_status` 函数bug——当部分任务有记录时，total取COUNT(*)而非len(tasks)，导致误判为"全部完成"而跳过执行。已修复为始终使用len(tasks)作为total。

### 2. 覆盖率计算口径调整
- `system_monitor.py`：`check_data_coverage` 排除已退市和北交所股票（8/920/43开头），避免每日误告警。
- 结果：有效活跃股票5,221只，排除643只（退市+北交所），覆盖率99.56%。

### 3. 缺失数据补全能力扩展
- `fill_missing_data.py`：新增 `--use-tushare` 参数，支持Tushare批量接口作为Baostock不可用时的降级方案。
- 新增 `_import_single_via_tushare` 和 `import_missing_via_tushare_batch` 函数，含前复权自动转换。

### 4. ETL 管道执行
- 执行阶段3全部8个任务，100%成功率，耗时950.9s。
- 数据质量：字段完整性100%，行情表与快照表差异0，技术指标覆盖5,168只，基本面覆盖5,525条。

## 数据质量

| 指标 | 值 |
|------|-----|
| 最新交易日 | 2026-07-21 |
| 日线字段完整性 | 100% |
| 覆盖率（排除退市/北交所） | 99.56% |
| 行情表↔快照表差异 | 0 |
| 整体健康度 | excellent |

## 新调度结构

```
阶段1 (15:30): 健康检查 + 股票列表同步          (2 任务)
阶段2 (17:45): 日线行情导入                      (1 任务)
阶段3 (18:15): 复权因子→补全→基本面→指标→        (8 任务)
               形态→信号→宽表→Parquet
```

## 明日计划
- 监控阶段3调度是否正常执行
- 持续关注23只缺失股票的覆盖情况