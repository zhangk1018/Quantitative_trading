# 方舟日报 2026-07-31

## 一、今日完成

### 1. 晨检
- 数据管道状态正常，最新导入 2026-07-30 的 5197 条数据（覆盖率 99.5%）
- 协作单无活动工单，.trae/topics.md 无新通知
- 量量昨日日报未提交

### 2. 定时任务 plist 修复（续昨日）
- **问题**：三个 plist 文件修改后 launchctl load 持续报 error 5 (EIO)，服务未加载
- **根因**：`/var/db/com.apple.xpc.launchd/disabled.501.plist` 中存在 9 条 stale 条目（含已废弃的 stage4/5、旧命名 daily_job、week-job-runner），导致 launchd 拒绝加载新 plist
- **修复**：
  - `plutil -remove` 因 key 含 `.` 被解析为嵌套路径，改用 Python `plistlib` 一次性删除所有 `com.quant.*` 条目
  - 重新 `launchctl load` 后三个服务全部正常加载
- **结果**：stage1(15:30)/stage2(17:30)/stage3(18:15) 均已生效，Weekday 2-6 (Mon-Fri)

### 3. sync_adj_factor 日志分析与修复
- 分析 `logs/sync_adj_factor.log` 中两类报错：
  - **复权因子异常大(>100)**：假警报。代码阈值 100 过低，000001/600601/600651 等上市 20-30 年老股正常累计因子达 10788（如 600653 申华控股），数据未被跳过，零影响
  - **689009 九号公司**：Sina 无该股 qfq-factor，fallback 到数据库前向填充，1 只股票不影响整体
- **修复**：`sync_adj_factor.py` 中 `expand_factor_to_daily` 的 adj_factor 警告阈值从 `>100` 提高到 `>50000`

## 二、明日计划
- 等待 K 安排

## 三、问题与风险
- 无