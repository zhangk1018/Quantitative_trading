# 量量日报 2026-08-27

## 后台负责模块
数据管道、ETL、数据库迁移

## 今日完成
按 ETL 阶段顺序组织

### 阶段1：前置检查
- 复权因子增量「只下载变化部分」优化（P1）：`sync_adj_factor.py` 新增 `get_recent_exright_codes()` / `_recent_report_periods()`，增量模式下用 `ak.stock_fhps_em()` 拉取最近 6 个季度末报告期全市场分红预案，筛选「除权除息日」落在增量窗口内的股票，仅对这些股票拉取复权因子，其余无除权股票直接跳过。
  - 实测 5232 只 → 212 只，调用次数减少约 24 倍
  - 分红事件接口异常时自动回退处理全部已有股票；新上市股票仍全量拉取；全量历史路径不受影响
  - 正确性验证通过：抽查 5 只窗口内除权股，库内 end_date 因子 == Akshare 直拉一致

### 阶段2：数据下载
- 定时任务时间调整：阶段2 由 17:30 → **16:30**（launchd stage2 plist 5 个工作日生效）

### 阶段3：数据计算
- 定时任务时间调整：阶段3 由 18:15 → **17:30**（launchd stage3 plist）

### 其他
- **全项目日志格式统一（P1）**：按统一规范（timestamp ISO8601 毫秒+时区 / LEVEL / logger / thread / message 5 字段）重构 `backend/utils/logger.py`（新增 `IsoFormatter` + `LOG_FORMAT` + `configure_root_logging`），并接入调度器 `daily_job_runner`、ETL 脚本（pattern/sync_stock_list/partition/daily_snapshot）、监控（monitor_download/download_monitor/utils/monitor）及后端 `main.py`。
  - 修正坑：`logging.basicConfig(datefmt='%Y-%m-%dT%H:%M:%S.%f%z')` 下 `strftime` 不解析 `%f`（毫秒变字面 `f`、时区无冒号），故基本配置文件统一改走 `IsoFormatter` 路径，全项目格式严格一致
  - 验证：样例输出 `2026-08-27T18:31:59.419+08:00 - INFO - pattern_precompute - MainThread - ✅ ...`，全部改动文件语法 OK
- **文档同步**：
  - `ETL_PIPELINE.md` v1.4 → v1.5：复权因子增量优化机制 + 定时时间（阶段2/3） + 常见问题补充
  - `量化交易.md` 新增第 7 章「日志规范（全项目统一）」：内容格式 / 文件路径命名（ETL→logs/{name}.log、调度→logs/cron/、后端→logs/backend/）/ 轮转
- 定时任务实施校验：launchd stage2/3 plist 时间已改并 reload，`plutil -lint` 通过；`load_launchd_plists.sh` echo 文案同步更新

## 数据库变更
- 无（本次无 SQL 迁移）

## 配置变更
- `scripts/launchctl/com.quant.daily_job_runner.stage2.plist`：17:30 → 16:30
- `scripts/launchctl/com.quant.daily_job_runner.stage3.plist`：18:15 → 17:30
- `scripts/load_launchd_plists.sh`：阶段描述文案同步

## 明日计划
- 验证新定时阶段2（16:30）/ 阶段3（17:30）首日自动触发正常
- 按要求继续推进数据管道与后台任务