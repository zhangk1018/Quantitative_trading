# 量量日报 2026-08-04

## 今日完成

### 1. ETL 管道阶段重构：sync_adj_factor 迁移至阶段1
- **背景**：阶段2日线导入时需使用复权因子转换前复权价格，但 sync_adj_factor 原在阶段3执行，存在时序问题
- **变更**：将 `adj_factor_sync` 从阶段3迁移至阶段1（15:30），在日线导入之前完成
- **影响文件**：
  - `backend/cron/daily_job_runner.py` (v10→v11)：STAGE1_TASKS 新增 adj_factor_sync，STAGE3_TASKS 移除
  - `backend/cron/scheduler_daemon.py`：更新阶段标签
  - `.trae/rules/ETL_PIPELINE.md` (v1.3→v1.4)：更新执行顺序、数据流向图、分阶段脚本

### 2. ETL 管道修复
- **signal_precompute 数据库连接失败**：根因 `username` 默认值为空字符串，导致 psycopg2 走 Unix socket 认证失败。修复为 `'quant_user'`，与 `compute_indicators_daily.py` 一致
- **文件锁异常处理**：FileLock 类新增僵尸锁自动检测与清理（`os.kill(pid, 0)`），区分存活进程与死进程的错误消息，release 时清理锁文件

### 3. 监控看板调整
- 复权因子监控项在 `TASK_CHAIN` 中前置至增量导入行情之前（A→B→D→C→E→F→G→H→I）
- `system_monitor.py` 中 `tables_to_check` 顺序同步调整
- `PIPELINE_STAGES` 中"复权因子"从"数据补全"移至"数据下载"

### 4. 调度守护进程
- 创建 `backend/cron/scheduler_daemon.py`，替代 launchd plist（本机 macOS 12.7.6 的 launchctl bootstrap/load 均返回 EIO 错误）
- 周一至周五 15:30/17:30/18:15 自动执行三个阶段

### 5. 今日数据完整性验证
- 全部 7 张核心表数据达标，Parquet 导出成功（5197条，92列）
- OHLCV 字段零空值，价格/成交量无异常
- signal_precompute 自动执行失败后手动重跑成功（4,460条信号）

## 今日问题

| 问题 | 状态 | 说明 |
|------|------|------|
| signal_precompute 自动执行失败 | 已修复 | 数据库连接 username 默认值错误，改为 `quant_user` |
| launchd 无法加载 plist | 已替代 | 创建 Python 调度守护进程作为替代方案 |

## 明日计划

- 验证阶段1 adj_factor_sync 在调度守护进程中正常执行
- 确认 signal_precompute 在调度守护进程中不再失败

## 代码变更清单

| 文件 | 操作 |
|------|------|
| `backend/cron/daily_job_runner.py` | 修改：阶段重构 v11 |
| `backend/cron/scheduler_daemon.py` | 新增：调度守护进程 |
| `backend/clean/etl/signal_precompute.py` | 修复：username 默认值 |
| `backend/core/api/router/monitor.py` | 修改：TASK_CHAIN 顺序 |
| `backend/monitoring/system_monitor.py` | 修改：tables_to_check 顺序 |
| `.trae/rules/ETL_PIPELINE.md` | 更新：文档 v1.4 |