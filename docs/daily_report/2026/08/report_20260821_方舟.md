# 方舟日报 — 2026-08-21

## 今日工作

### 1. 选股视图 — 量化评分列 + 自编指标评分管线
- **新增"量化评分"列**：`StockPickerTable` 移除"K线形态"列，新增"量化评分"列渲染自编指标评分
- **评分数据管线**：`CustomIndicatorService.computeAndFilter` 返回 `scores` Map，`useScreenerData` 映射到 `StockItem.custom_score`，`StockItem` 类型新增 `custom_score?: number`
- **自编指标种子**：`customIndicatorSeed.ts` 新增 `seed8ConditionIndicator()` 函数，在 `ScreenerProvider` 启动时自动注册"8条件选股改进"自编指标
- **浏览器自测通过**：5204 只股票全部完成计算，筛选出 22 只，评分 6.0~8.0，平均 6.7，量化评分列显示正常

### 2. Pyodide Worker 初始化竞态条件修复
- **`customIndicatorRunner.ts`**：`init()` 增加 `initPromise` 缓存，避免重复初始化导致 Worker 状态不一致
- 修复 "Pyodide Worker 未就绪，请先调用 init()" 报错

### 3. 交易计划股票搜索 Bug 修复
- **`pdca/services/stock.ts`**：`searchStocks()` 函数移除多余的 `.items` 访问，后端返回纯数组，Axios 拦截器已自动解包
- 修复 "股票搜索失败，请检查网络连接" 报错
- 影响范围：`TradingPlanEditor.tsx` 两处 + `TradingRecordForm.tsx` 一处

### 4. 选股分层止盈缺省参数对齐
- **`engine.ts`** 与 **`BacktestPanel.tsx`** 高级设置缺省参数 10 项差异对齐（`initialStopLossPct`、`firstProfitPct`、`maxHoldDays` 等），确保用户看到的参数与实际执行结果一致

### 5. 其他前端修复
- **`TradingConfigPanel.tsx`**：交易成本配置优化
- **`patternDetector.ts`**：K 线形态检测逻辑微调
- **`StrategyBacktestView.tsx`**：回测分析视图调整

## 问题
- 无阻塞问题

## 明日计划
- 等待 K 安排新任务