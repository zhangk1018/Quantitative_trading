# 方舟日报 2026-08-18

## 前台负责模块
PDCA 交易自律系统前端

## 今日完成

### 1. API 架构重构（P0，基于 K 代码审阅建议）
- 创建 `services/client.ts`：统一 Axios 实例 + 响应拦截器自动解包（code=200 直接返回 data）+ 自定义 `NetworkError`/`BusinessError` 异常类
- 创建 `services/factory.ts`：泛型 CRUD 工厂函数 `createEntityService<T>` / `createReadonlyService<T>`，消除重复代码
- 按业务领域拆分为 13 个独立 service 文件（record/cycle/plan/check-report/act-record/diary/snapshot/config/import/stock/behavior-log）
- `api.ts` 降级为 barrel 导出，保持向后兼容
- 修复 `parseImportExcel` 绕过 Axios 使用原生 fetch 的问题

### 2. 调用方迁移（P1）
- 更新 18 个组件/页面，移除所有 `res.code` 手动检查，统一使用响应解包后的数据
- `usePDCACycle` hook 改用 `useCallback` 包裹 `loadCycles`，声明正确依赖项

### 3. 类型分层（P2）
- 创建 `constants.ts`，将 7 组 LABELS/OPTIONS 从 `types.ts` 迁出
- 更新 6 个组件导入路径，type-only 导入保持 `../types`

### 4. 编译修复
- 修复 `TradingRecordTable.tsx` 错误导入（`exportRecords`/`downloadExcel` 从错误模块导入）
- 修复 `TradingPlanEditor.tsx` 未使用的 `transitionCycle` 导入
- 修复 `TradingRecordForm.tsx` 未使用的 `batchResult` 变量
- 修复 `client.ts` 未使用的 `API_PREFIX` 导入
- 修复 `factory.ts`/`plan.ts` 类型断言（AxiosResponse 直转业务类型需 `unknown` 桥接）

## 变更文件
- 16 文件变更，+434 / -938 行（净减少 504 行，架构重构后代码量显著下降）
- 新增 14 个文件：`services/` 下 13 个 + `constants.ts`

## 明日计划
- 等待 K 审核本次架构重构，根据反馈调整
- 处理后续待办（服务端排序、Token 安全存储等如有需要）