# 方舟日报 2026-08-25

## 前台负责模块
经验知识库浏览界面、PDCA 前端、协作单联调验证

## 今日完成

### 1. 经验知识库浏览界面（新功能，P1）
- 新增 PDCA「经验知识库」页签，只读浏览冻结经验：列表 + 标签筛选 + 关键词搜索 + 分页 + 详情折叠展开
- 前端 4 文件：
  - `PDCADashboard.tsx`：新增「经验知识库」Tab（改进措施之后），引入 ExperienceLibrary 组件与 BulbOutlined 图标
  - `types.ts`：新增 `TradeExperience` / `ExperienceQueryParams` 类型
  - `services/experience.ts`：`fetchExperiences()` Service 层（与后端契约解耦）
  - `components/ExperienceLibrary.tsx`：列表卡片 + 标签下拉筛选(多选) + 搜索框 + 分页 + 详情 Collapse + 竞态防护 + 空态兜底
- `npx tsc --noEmit` 通过；浏览器实测页签渲染 / 标签 / 详情 / 搜索全正常，console 无 JS 报错

### 2. 协作单 [21.0] 经验知识库查询 API（提单 + 联调验证）
- 提单（NEW）：定义 `GET /api/pdca/experiences` API 契约（tags 标签筛选 / keyword / 分页 / 过滤软删除），转量量实现
- 量量实现（VERIFY）后联调验证：curl 全量 / 标签 `tags[]=` / keyword 三项全过；浏览器实测经验卡片正确展示

### 3. 协作单 [22.0] 资金曲线数据不正确 bug（诊断 + 提单 + 复核）
- 定位根因：`snapshots.py` `get_auto_equity_curve` 未平仓浮盈 unrealized 用「每只股票最新收盘价」计算，导致全时段恒定
- 交叉验证交易台账 + 行情确认根因，提单转量量修复
- 量量修复（按事件日收盘价序列 + bisect 前向填充）后复核通过：后端已重启加载新代码，浏览器资金曲线渲染正常、4 统计卡片正确、时间段切换可用

### 4. 协作单 [23.0] 数据管道回归修复（验证）
- 量量修复 pandas 2.x + SQLAlchemy 2.x 参数化查询回归（`_run_query_df` helper）
- 方舟验证通过：代码 10 处替换到位、read_sql 无残留；今日管道 task_run_log 全 success；关键表实测 688117 quotes 1269 / 指标 223 / 宽表 5208 / 信号 3006（最新 08-25）

## 变更文件
- `frontend/src/features/pdca/types.ts`（新增 TradeExperience 类型）
- `frontend/src/features/pdca/services/experience.ts`（新增）
- `frontend/src/features/pdca/components/ExperienceLibrary.tsx`（新增）
- `frontend/src/features/pdca/PDCADashboard.tsx`（新增经验知识库页签）
- `docs/协作单.md`（21.0/22.0/23.0 闭环记录）
- `.trae/topics.md`（跨会话通知）

## 明日计划
- 根据 K 需求排期前端新任务
- 关注量量遗留的数据管道增量优化

## 交叉复核备注
- 量量日报「完成」项：23.0 数据管道恢复已抽样验证通过；22.0 资金曲线修复已独立核算一致
- 量量日报「问题」项：无阻塞，前端改动由本人提交