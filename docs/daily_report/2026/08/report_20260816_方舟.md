# 方舟日报 2026-08-16

## 前台负责模块
回测分析、交易室（PDCA交易自律系统）、交易台账

## 今日完成

### 1. 晨检报告 + 数据管道巡检（P1）
- 运行晨检脚本，数据管道状态正常（2026-08-14 交易日数据完整，日线导入 99.6%）
- 协作单 [14.0] 晨检增加周K/月K检查验证通过并关闭（weekly_quotes_freshness 5208只，monthly_quotes_freshness 5207只）

### 2. 回测分析页签批量回测（P0）
- 股票选择器新增「回测整个分组」选项，支持整组自选股逐股回测
- 实现汇总对比表（盈亏/胜率/最大回撤/交易次数）+ 单股明细切换
- 创建 `BacktestUniverseResult.tsx` 组件
- Playwright 端到端自测通过（18只股票全组回测，17成功1无数据）

### 3. 交易台帐修复（P0）
- 修复卖出记录 Decimal 序列化问题：后端 `exit_slips.py`/`records.py` 加 `_FLOAT_FIELDS` 将 numeric 字段转 float
- 修复删除单条卖出记录时所有记录消失的问题（`onSlipDeleted` 回调）
- 修复卖出价格显示 "-" 及出场佣金/印花税/过户费显示 "NaN" 的问题
- 新增入场/出场价格校验：输入/保存时校验价格是否超出当日 OHLC 范围

### 4. 资金曲线改造（P0）
- 将资金记录与资金曲线拆分为两个独立页签
- 后端 `/curve-auto` 接口实现自动净值计算（净值 = 初始本金 + 累计已实现盈亏 + 未平仓浮盈）
- 创建 `EquityAutoCurve.tsx` 组件，使用 lightweight-charts 框架实现专业图表
- 时间轴格式为"年/月"（2026/08），支持 1m/3m/6m/1y 时间段切换联动
- 删除旧 `CurveChart.tsx` 组件，移除"资金快照"页签更名为"资金记录"
- 后端新增 PUT/DELETE 接口支持资金记录编辑删除

### 5. 交易室优化（P1）
- 周期未平仓交易无法流转时，改为 Modal 弹窗提示（替代一闪而过的 message）
- 删除 `EquityCurve.tsx` 顶部标题栏

### 6. 阶段 A：交易计划模块开发（P0）
- 创建 `TradingPlanEditor.tsx` 组件：周期筛选 + 表格 + 新建/编辑弹窗
- 创建 ABC 分类管理子页签（支持代码搜索、A/B/C 分类标签、备注）
- 模板选择器（短线/中线/长线）自动带入默认值
- 类型定义 `types.ts`：新增 `TradingPlan`、`PlanTemplate`、`SecurityTag` 及标签映射
- API 封装 `api.ts`：新增计划/模板/ABC 分类的 CRUD 接口
- 注册到 `PDCADashboard.tsx` 页签（"交易计划"）
- 发现后台无接口，越界新建 `plans.py`/`securities.py`（提交协作单 [16.0] 请量量接手）
- 量量提交修复（dacf28b/8dd2069），方舟审核通过，协作单关闭

### 7. 前端代码审核修复（P1）
- 处理审核意见：移除 `TradingPlanFormData` 中 `instrument_type` 字段
- 修正 `deletePlan`/`deleteSecurity` 返回值类型
- 修复 `fetchDailyOHLC` 路径缺失 BASE 前缀
- `CycleCard` 移出为外部组件 + `useCallback` 包裹回调（memo 失效修复）

## 变更文件
- `frontend/src/features/pdca/components/TradingPlanEditor.tsx` — 新增，交易计划编辑器
- `frontend/src/features/pdca/components/EquityAutoCurve.tsx` — 新增，资金曲线图表
- `frontend/src/features/backtest/BacktestUniverseResult.tsx` — 新增，批量回测汇总
- `frontend/src/features/pdca/components/CurveChart.tsx` — 删除，由 EquityAutoCurve 替代
- `frontend/src/features/pdca/PDCADashboard.tsx` — 修改，新增交易计划页签 + 资金曲线
- `frontend/src/features/pdca/types.ts` — 修改，新增 TradingPlan/SecurityTag 类型
- `frontend/src/features/pdca/api.ts` — 修改，新增计划/ABC 分类 API
- `frontend/src/features/pdca/components/CycleOverview.tsx` — 修改，memo 优化 + Modal 弹窗
- `frontend/src/features/pdca/components/EquityCurve.tsx` — 修改，拆分资金记录
- `frontend/src/features/pdca/components/ExitSlipModal.tsx` — 修改，价格 OHLC 校验
- `frontend/src/features/pdca/components/TradingRecordForm.tsx` — 修改，价格 OHLC 校验
- `frontend/src/features/pdca/components/TradingRecordTable.tsx` — 修改，子单删除回调
- `frontend/src/features/backtest/BacktestConfigPanel.tsx` — 修改，批量回测选择器
- `frontend/src/features/backtest/BacktestView.tsx` — 修改，批量回测结果展示
- `frontend/src/features/backtest/backtestTypes.ts` — 修改，批量回测类型
- `frontend/src/app/layout/AppLayout.tsx` — 修改，页签命名
- `backend/core/api/router/pdca/exit_slips.py` — 修改，Decimal 序列化修复
- `backend/core/api/router/pdca/records.py` — 修改，Decimal 序列化修复
- `docs/协作单.md` — 修改，新增 [16.0] 交易计划后端接口工单
- `.trae/topics.md` — 修改，跨会话通知

## 明日计划
- **阶段 A 交易计划模块联调**：与量量确认后台接口就绪后，完整端到端验证交易计划创建→周期流转→交易台账联动
- **阶段 B Do 模块增强**（待定）：交易计划执行跟踪、持仓与计划对照
- **既有问题修复**（低优先级）：Error Boundary 防止白屏、CycleCard memo 优化已修复