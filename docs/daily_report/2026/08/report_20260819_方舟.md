# 方舟日报 — 2026-08-19

## 今日工作

### 1. 协作单 [18.0-FRONTEND-AUDIT-20260819] 前端代码审计整改
- **Q1/Q3**: `client.ts` 新增 `apiGet<T>`/`apiPost<T>` 等泛型函数，9 个 service 文件全部迁移，消除 `as unknown as` 双重断言
- **Q2**: `factory.ts` 合并 `list`/`listAll` 重复实现
- **Q4**: `fetchDailyOHLC` 移除 `try/catch`，异常自然抛出，调用方区分"无数据"与"请求失败"
- **Q5**: `usePDCACycle` 使用 `DEFAULT_STATUS_ORDER` 常量归一化依赖数组，消除 `eslint-disable`
- **P1**: `customIndicatorRunner` 按批次计算最大天数，避免全局预分配内存膨胀
- **S2**: Worker 输出限制 `MAX_OUTPUT_ELEMENTS=500000`，增强安全性
- **补迁移**: `diary.ts`/`check-report.ts` 迁移到泛型 API
- **量量终审复核**: 修复 Q5 无限循环（`CheckModule`/`ActModule` 内联对象字面量改为模块级常量）和 S2 校验失效（`totalElements` 改为 `reduce` 求和 + `Array.isArray` 守卫）
- **状态**: 量量终审通过 → CLOSED

### 2. K 代码审阅 — 4 项高优先级修复
- **错误提取工具函数**: `extractErrorMessage(err, fallback)` 优先提取 `BusinessError.serverMessage` → `NetworkError.message` → `Error.message` → 默认文案
- **useEffect 竞态防护**: `ActModule`/`CheckModule` 引入 `fetchingRef`（`useRef`），清理函数标记过期，异步回调中忽略过期请求
- **handleDelete 乐观更新回滚**: 删除前保存 `snapshot`，接口失败时 `setRecords(snapshot)` 恢复
- **undefined 值传递**: `CheckModule` 中 `execSummary` 字段赋值加 `?? null` 守卫

### 3. K 代码审阅 — 第二轮 3 项高优先级修复
- **handleSave 乐观更新回滚**: `ActModule`/`CheckModule` 更新场景先 `setRecords`/`setReport` 乐观更新，失败时 `fetchActRecords`/`fetchCheckReport` 全量拉取回滚
- **表单验证错误误判**: 替换脆弱的 `err.message?.includes('validate')` 为 `'errorFields' in err`，精准识别 Ant Design 验证错误，避免双重报错误导用户
- **401 无限循环防护**: `client.ts` 拦截器增加 `pathname !== '/login'` 判断 + 请求级 `skipAuthRedirect` 配置项

### 4. 协作单 [19.0-AUTH-FRONTEND-20260819] 前端认证迁移
- `client.ts`: 删除 `localStorage` token 逻辑，`withCredentials: true`，401 跳转 `/login`
- `stock-detail/api.ts`、`watchlist/api.ts`: 各 `axios.create` 加 `withCredentials: true`
- 新建 `Login.tsx`: 全屏居中登录页，调 `/api/auth/login`，Enter 键提交
- `router.tsx`: 新增 `/login` 路由 + `AuthGuard` 路由守卫（`/api/auth/verify` 探活）
- 量量终审 + K 审阅通过，后端已重启生效

## 问题
- 无阻塞问题

## 明日计划
- 无待办，等待 K 安排新任务