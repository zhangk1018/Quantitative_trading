# 量量日报 2026-08-24

## 后台负责模块
数据管道、ETL、数据库迁移、后端服务管理

## 今日完成

### 数据管道（阶段1→3）
- 每日晨检：数据库/后端/ETL 服务正常（OK=10, WARN=1 为 8-21 历史失败任务，数据已补全）
- 阶段2：8-24 日线数据导入成功，**5207 只**（晨检 + SQL 双验证）
- 阶段3：8-24 技术指标/宽表同步完成，指标覆盖率 >99%（dif 99.98%、rsi6 99.5%），技术 pattern 正常（ma_long 1147 / macd_golden 60 / macd_bottom 132）
- 数据质量核查通过，**数据完好**

### 1. 冻结经验落库（P1）
- V006 迁移：`pdca.trade_experience` 增加 `source_act_record_id` 列 + 唯一索引（幂等键）
- `act_record.py` 新增 `_sync_trade_experience`：冻结开启自动生成经验条目、关闭软删除、删除连带软删除
- 单元测试 3/3 通过（mock 数据库），curl 全链路自测通过

### 2. 后端统一 launchd 管理（P1）
- 排查后端反复崩溃（`com.quant.backend` 退出码 3）：根因 PostgreSQL 未运行，launchd KeepAlive 反复拉起失败
- 修复：数据库/后端/ETL 统一**用户域 launchd**（`~/Library/LaunchAgents`），登录后自动加载
- 停用 `backend_watchdog.sh` 与 `backend.healthcheck`（消除多套拉起抢端口），healthcheck plist 已删除
- 数据库开机自启：新增 `com.quant.postgresql`（指向项目数据目录）
- 验证：重启后服务自愈，健康检查 200

### 3. 错误码统一（P1）
- 新建 `backend/shared/error_codes.py`：集中式错误码表（`ErrorCode` 含 code/message/category/source 四要素，`DataSource` 枚举 baostock/tushare/akshare/pytdx/postgresql/local）
- PDCA 10 个路由文件（records/diaries/snapshots/import_export/exit_slips/config/act_record/cycles/plans/check_report/behavior_log）业务错误码全部统一引用
- API detail 保持 `"CODE: message"` 兼容前端；日志 `log_msg()` 输出 `[category/source]` 便于定位数据源

### 4. 日志规范（P1）
- 分层目录：`logs/{backend,etl,cron,postgres,system,frontend}`
- 后端/数据库 launchd 日志路径更新，命名规则 `{服务}/{模块}.log`

### 5. 日志清理（P2）
- 随月K线聚合执行：`compute_bar_aggregation.py` 新增 `cleanup_expired_logs()`（保留 60 天），月线聚合完成后自动清理
- 删除独立定时任务（cleanup_logs.sh + log-cleanup plist）

### 6. stage2 触发时间修正（P2）
- 发现 plist 配置 17:45 与 ETL 文档 17:30 不一致，已改为 **17:30**

### 7. Pandas DBAPI2 连接警告修复（P2）
- `postgresql_storage.py` 12 处 `pd.read_sql` 由 psycopg2 原生连接改用 SQLAlchemy engine（NullPool，避免双池叠加）
- 消除 "pandas only supports SQLAlchemy connectable" 警告；实测 `%s` 占位符 + 元组参数完全兼容

### 8. Baostock pe_ttm 补全修复（P1）
- **日期格式修复**：`bs_date` 由 `YYYYMMDD` 改为 `YYYY-MM-DD`（Baostock 要求），消除"日期格式不正确"导致补全 0 成功
- **超时防护**：新增 10 秒 `signal.alarm` 超时机制（`_BaostockTimeout`），Baostock 网络阻塞时单只最多等 10 秒跳过，避免补全卡死（此前曾运行 1 小时 9 分钟卡死）
- 实测验证：补全 1498/1552 只成功（99.6%），pe_ttm 覆盖率 72% → **99.0%**，全程 6.5 分钟无卡死

## 数据库变更
- `backend/db/migrations/pdca/V006__add_trade_experience_source.sql`：trade_experience 增加 source_act_record_id 列 + 唯一索引

## 配置变更
- `scripts/start.sh`：dev start/stop/restart/status 适配用户域 launchd；dev stop 不碰数据库
- `scripts/load_launchd_plists.sh`：一键复制+加载用户域服务
- `scripts/launchctl/`：backend/postgresql/stage2 plist 更新，删除 healthcheck，新增 postgresql

## 明日计划
- 待 K 安排
