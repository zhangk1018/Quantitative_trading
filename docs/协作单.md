# 协作单

> 方舟与量量之间的工单系统，基于增强黑板模式。
> 最后更新：2026-07-28

***

## 工作流

### 状态机

```
NEW（方舟/量量提单）
  │
  ▼
ASSIGNED（量量/方舟 认领并开始处理）
  │
  ▼
VERIFY（处理完成，待对方验证）
  │
  ├─ ✅ → CLOSED（验证通过，关闭）
  │
  └─ ❌ → REOPENED（验证不通过 → 回到 ASSIGNED）
```

### 各角色职责

| 角色     | 在工单中的职责                                                 |
| ------ | ------------------------------------------------------- |
| **方舟** | 发现前端/后端问题 → 提单（NEW）；量量修复后做前端侧验证（VERIFY→CLOSED/REOPENED） |
| **量量** | 修复后端/数据问题（ASSIGNED→VERIFY）；方舟提的后端问题，量量认领修复              |
| **K**  | 仲裁争议（REOPENED 后双方无法达成一致时）；最终验收                          |

### 生命周期规则

1. **提单方**（通常是发现方）写入工单时状态为 `NEW`
2. **处理方**认领后改为 `ASSIGNED`，开始修复
3. **处理方**完成修复并验证后改为 `VERIFY`，**同时**在 topics.md 追加一行通知对方
4. **验证方**从前端/使用侧验证后：

   - 验证通过 → `CLOSED`

   - 验证不通过 → `REOPENED`，注明原因，**同时**在 topics.md 追加一行通知处理方
5. 一个工单最多 REOPENED 2 次，第 3 次需要 K 介入仲裁

***

## 通知约定

每次状态变更，**除了更新协作单本身**，同步在 [.trae/topics.md](file:///Users/zhangk/workspace/Quantitative_trading/.trae/topics.md) 追加一行：

```
[操作方→接单方 YYYY-MM-DD HH:mm] 协作单 [ID] 状态变更: OLD→NEW（摘要）
```

**关键**：必须包含"接单方"，明确下一个动作的接收方，避免读通知时不知道该谁接手。

***
## \[31.0-KLINE-EXDIV-ANNOTATE-20260903] K线除权日标注（前端增强，后置待 K 确认）

| 字段       | 内容                           |
| -------- | ---------------------------- |
| **状态**   | **NEW（登记在案，暂不处理）**           |
| **提单方**  | 方舟                           |
| **日期**   | 2026-09-03                   |
| **影响范围** | frontend（K线详情 useStockChart） |
| **优先级**  | P2（增强，后置）                    |

**背景**：K 批示「K线除权日标注：后置，等 K 确认；协作单记录，暂不动」。

**说明**：M6 前端可选增强（方案 §1.2；除权日 `factor_date` 因子由量量 M2 输出）——在 K 线顶部/图例标注 `factor_date` 除权除息日，辅助识别复权口径下的价格跳变。**非 M6 必须项**，本期暂不实现，仅登记备案。

**待 K 确认**：是否纳入，以及纳入时的优先级/排期。

***


## [33.3-HK-FACTOR-RESIDUAL-20260910] 港股 factor 残留类型口径复核（f=0 型 0021 + 疑似类型，请量量复核）

| 字段 | 内容 |
| --- | --- |
| **状态** | **NEW** |
| **提单方** | 方舟 |
| **处理人** | 量量 |
| **日期** | 2026-09-10 |
| **影响范围** | backend / 港股数据源（`stock_adj_factor`） |
| **优先级** | P2 |

**背景**：协作单 [32.0-HK-UNADJUSTED-20260907] 已闭环（存量"未复权 adj≈close"错价经量量补清后归零 ✅，方舟终验通过 CLOSED 归档）。清理过程中排除出两类**非本单范围**的 factor 残留，登记待量量口径复核：

- **f=0 型**（数据缺失/停牌/除权占位）：`0021.HK` 2026-04-08 / 2026-05-08 等多行 factor=0（复权因子为 0 ≠ 未复权错价，独立类型）。
- **疑似类型**（因子非≈1，可能为真实复权段/除权/低因子波动，需甄别是否错价）：`0206.HK`(2026-06-30 f=0.58, 前后 0.89/0.91)、`0339.HK`(2025-05-20 f=0.32, 该段整段 0.3~0.6 波动)、`0612.HK`(2025-06-25 f=0.39, 前后 3.00/1.47)、`3301.HK`(2026-08-05 f=9.28, 前后 21/14, 序列 08-04→08-06 连串下移疑除权)。

**预期**：请量量确认上述行——`0021.HK` 的 f=0 是否影响不复权→前复权转换或除权注释；0206/0339/0612/3301 是否属真实复权段/除权（无需处理）或确需处置。若均无需处理，登记口径结论即可。

**验收方式**：量量给出各类型口径判定与处置说明（或"无需处理"结论）。

**处理记录**:
- 2026-09-10 方舟: 提单（NEW），源自 32.0 清理排除项。
