# 协作单

> 方舟与量量之间的工单系统，基于增强黑板模式。
> 最后更新：2026-07-28

---

## 工作流

### 状态机

```
NEW（方舟/量量提单）
  │
  ▼
ASSIGNED（量量/方舟 认领并开始处理）
  │
  ▼
VERIFY（处理完成，待对方验证）
  │
  ├─ ✅ → CLOSED（验证通过，关闭）
  │
  └─ ❌ → REOPENED（验证不通过 → 回到 ASSIGNED）
```

### 各角色职责

| 角色 | 在工单中的职责 |
|------|---------------|
| **方舟** | 发现前端/后端问题 → 提单（NEW）；量量修复后做前端侧验证（VERIFY→CLOSED/REOPENED） |
| **量量** | 修复后端/数据问题（ASSIGNED→VERIFY）；方舟提的后端问题，量量认领修复 |
| **K** | 仲裁争议（REOPENED 后双方无法达成一致时）；最终验收 |

### 生命周期规则

1. **提单方**（通常是发现方）写入工单时状态为 `NEW`
2. **处理方**认领后改为 `ASSIGNED`，开始修复
3. **处理方**完成修复并验证后改为 `VERIFY`，**同时**在 topics.md 追加一行通知对方
4. **验证方**从前端/使用侧验证后：
   - 验证通过 → `CLOSED`
   - 验证不通过 → `REOPENED`，注明原因，**同时**在 topics.md 追加一行通知处理方
5. 一个工单最多 REOPENED 2 次，第 3 次需要 K 介入仲裁

---

## 通知约定

每次状态变更，**除了更新协作单本身**，同步在 [.trae/topics.md](file:///Users/zhangk/workspace/Quantitative_trading/.trae/topics.md) 追加一行：

```
[操作方→接单方 YYYY-MM-DD HH:mm] 协作单 [ID] 状态变更: OLD→NEW（摘要）
```

**关键**：必须包含"接单方"，明确下一个动作的接收方，避免读通知时不知道该谁接手。

---

## [8.0-CODE-AUDIT-20260728] 方舟 6 维度后台代码审核 — P0 5 项紧急修复

| 字段 | 内容 |
|------|------|
| **状态** | **CLOSED** |
| **提单方** | 方舟 |
| **日期** | 2026-07-28 |
| **影响范围** | backend |
| **优先级** | **P0** |

**定位路径**: 见下方逐项说明

**问题描述**:

方舟于 2026-07-28 对 `backend/` 全部 Python 文件进行了 6 维度（代码规范/错误处理/SQL安全/安全性/性能/可维护性）全面审核，发现 **P0 严重问题 5 项**，如下：

#### P0-1：硬编码数据库密码 — `backend/tools/draw_kline.py:8`
```python
CONN = dict(host="localhost", port=5432, dbname="quant_trading", user="quant_user", password="990518")
```
明文真实密码 "990518" 硬编码在代码中，存在泄露风险。

#### P0-2：硬编码默认密码 — `backend/collector/etl/health_monitor.py:152/174`
```python
env={**os.environ, 'PGPASSWORD': os.environ.get('PG_PASSWORD', '990518')}
```
默认值 "990518" 暴露真实密码，2 处。

#### P0-3：bare except 吞没异常 — `backend/core/service/data_service.py:756`
```python
def validate_connection(self) -> bool:
    try:
        stats = self.storage.get_stats()
    except:
        return False
```
`validate_connection()` 是健康检查关键路径，`get_stats()` 失败被静默吞没，上游无法感知真实错误原因。

#### P0-4：bare except 静默返回 None — `backend/core/service/kline_service.py:363`
```python
try: return Decimal(str(round(float(val), digits)))
except: return None
```
Decimal 转换失败时静默丢弃，应记录具体异常类型（TypeError/ValueError）。

#### P0-5：f-string 拼接 SQL 存在注入风险 — `backend/clean/processor/base_importer.py:133`
```python
sql = f"UPDATE task_progress SET {', '.join(update_fields)} WHERE id = %s"
```
`update_fields` 列表来自方法参数，虽当前调用方均传入硬编码字符串，但架构上存在注入风险。需改为白名单校验。

**预期修复**:

| 编号 | 修复方式 |
|------|----------|
| P0-1 | `draw_kline.py` 改为 `os.getenv('PG_PASSWORD')` 从环境变量读取 |
| P0-2 | `health_monitor.py` 默认值改为空字符串 `''`，密码为空时跳过 psql 调用 |
| P0-3 | `data_service.py` 改为 `except Exception as e:`，至少 `logger.error(f"连接验证失败: {e}")` |
| P0-4 | `kline_service.py` 改为 `except (TypeError, ValueError) as e:`，`logger.warning(f"Decimal转换失败: {val}, {e}")` |
| P0-5 | `base_importer.py` 对 `update_fields` 做白名单校验（如 `allowed_fields = {'status', 'progress', 'message'}`），或使用 `sql.Identifier` 包装列名 |

**验收方式**:
1. `grep -rn "990518" backend/` 返回 0 结果
2. `grep -rn "quant_password" backend/` 返回 0 结果
3. `grep -rn "except:" backend/` 中 `data_service.py` 和 `kline_service.py` 不再出现
4. `base_importer.py:133` 的 SQL 不再使用 f-string 直接拼接列名

---

**处理记录**:
- 2026-07-28 方舟: 提单（NEW）—— 6 维度代码审核，发现 P0 5 项、P1 19 项、P2 6 项，本工单聚焦 P0 修复
- 2026-07-28 量量: 修复 P0-1~P0-5（未走流程更新状态）
- 2026-07-28 方舟: 验证（REOPENED）—— 验收项 1/3/4 通过，验收项 2 不通过：`grep -rn "quant_password" backend/` 仍有 5 处默认值，分别在 calculate_highs.py:29、postgresql_storage.py:109、export_parquet.py:77、run_data_complete.py:215、storage_factory.py:38。需改为空字符串或从环境变量读取。
- 2026-07-28 量量: 修复完成（REOPENED→VERIFY）—— 5 处 quant_password 改为空字符串，pyc 缓存已清理，grep 990518 和 quant_password 均为 0。请方舟验证。
- 2026-07-28 方舟: 验证通过（VERIFY→CLOSED）—— 4 项验收全部通过：990518 0 匹配、quant_password 0 匹配、bare except 已清除、base_importer 白名单校验到位。

---

## [9.0-WEEKLY-MONTHLY-KLINE-20260809] 周K线/月K线数据接口就绪

| 字段 | 内容 |
|------|------|
| **状态** | **CLOSED** |
| **提单方** | 量量 |
| **日期** | 2026-08-09 |
| **影响范围** | frontend |
| **优先级** | **P1** |

**定位路径**: 后端 API + 数据库

**问题描述**:

量量已完成周线/月线K线数据聚合计算，并配置了定时任务：
- 周线计算：每周最后一个交易日 19:00 执行，cycle=`1w`
- 月线计算：每月最后一个交易日 20:00 执行，cycle=`1m`

后端已就绪的接口和数据：

**1. K线数据接口**（已有，无需新建）
```
GET /api/kline/{code}?period=weekly&start_date=&end_date=&limit=1000
GET /api/kline/{code}?period=monthly&start_date=&end_date=&limit=1000
```
- `period` 参数支持 `daily` / `weekly` / `monthly`（已在 [kline.py](file:///Users/zhangk/workspace/Quantitative_trading/backend/core/api/router/kline.py) 硬编码 VALID_KLINE_PERIODS）
- 返回格式与日线K线完全一致（含 trade_date, open, high, low, close, volume, amount, pe_ttm, turnover_rate 等字段）
- 前端调用方式：`fetchKline(code, 'weekly', startDate, endDate, limit)` 或 `fetchKline(code, 'monthly', ...)`

**2. 数据存储位置**
- 周线数据：`stock_quotes` 表，`cycle='1w'`，键 `(code, cycle, trade_date, adjust_type)`
- 月线数据：`stock_quotes` 表，`cycle='1m'`，键 `(code, cycle, trade_date, adjust_type)`

**3. 当前数据覆盖**
- 周线：最新 2026-08-07，5202 只股票，385 周历史
- 月线：最新 2026-07-31，5207 只股票，139 个月历史

**4. 数据聚合原理**
- 基于日线数据（前复权）聚合生成
- OHLC：周/月开盘=周期内首日开盘，最高=周期内最高，最低=周期内最低，收盘=末日收盘
- 成交量/成交额：周期内累加

**预期前端操作**:

方舟需在前端 K线图表组件中支持周期切换（日/周/月），当用户选择"周K线"或"月K线"时，调用 `fetchKline(code, 'weekly', ...)` 或 `fetchKline(code, 'monthly', ...)` 获取对应周期数据并渲染。

**验收方式**:
1. 在 K线页面选择"周K线"，确认显示周K线蜡烛图（示例：000001 平安银行应有 385 周数据）
2. 在 K线页面选择"月K线"，确认显示月K线蜡烛图（示例：000001 平安银行应有 139 个月数据）
3. 周线/月线的 OHLC 数据逻辑正确（开盘=周期首日，最高/最低=周期内极值，收盘=末日）

---

**处理记录**:
- 2026-08-09 量量: 提单（NEW）—— 周线/月线数据聚合已完成，定时任务已配置，后端 API 已就绪
- 2026-08-09 方舟: 认领，开始处理（ASSIGNED）—— 1. 将周线/月线加入每日巡检；2. 前端K线图增加周期切换
- 2026-08-09 方舟: 修复完成，待验证（VERIFY）—— 1. pipeline_health_check.py 新增周K线(cycle='1w')/月K线(cycle='1m')最新日期和覆盖率检查；2. StockAnalysisModal.tsx 新增日K/周K/月K Segmented 切换按钮，调用 fetchKLineData 传入 period 参数；前端构建通过，后端API验证通过，每日巡检脚本验证通过
- 2026-08-11 量量: 验证通过（VERIFY→CLOSED）—— 后端API：周K线 500+ 条、月K线 139 条，数据正常；浏览器自测：日K/周K/月K 三段切换均正常渲染，无JS错误

---

## [10.0-PDCA-PHASE1-BACKEND-20260810] PDCA 一期"基础台账 MVP"后端建表+数据存取接口

| 字段 | 内容 |
|------|------|
| **状态** | **CLOSED** |
| **提单方** | 方舟 |
| **日期** | 2026-08-10 |
| **影响范围** | backend（数据库 + API） |
| **优先级** | **P0** |

**定位路径**: 新建 `backend/core/api/pdca/` + `backend/core/models/pdca/` + `backend/db/migrations/pdca/`

**背景**：PDCA 交易自律迭代系统一期"基础台账 MVP"正式启动。方舟负责前端开发，量量负责后端建表和数据存取接口。

**需求描述**：

### 一、数据库迁移（V001）

执行 `backend/db/migrations/pdca/V001__init_pdca_schema.sql`，创建 `pdca` schema 下全部 14 张表 + 枚举类型 + 索引 + 触发器。完整 DDL 见 `docs/PDCA_交付物一_数据库字段表.md` 第 2 节。

一期核心表（前端直接依赖）：
- `pdca.account` — 账户主表
- `pdca.system_config` — 系统配置
- `pdca.account_snapshot` — 资金快照
- `pdca.pdca_cycle` — PDCA 周期
- `pdca.trading_record` — 交易台账（核心）
- `pdca.trading_diary` — 交易日记
- `pdca.broker_adapter` — 券商适配器
- `pdca.behavior_log` — 行为/违规日志

### 二、后端路由模块骨架

创建 `backend/core/api/pdca/` 目录，包含：
- `__init__.py` — 路由注册
- `records.py` — 交易台账 CRUD API
- `snapshots.py` — 资金快照 + 资金曲线 API
- `diaries.py` — 交易日记 CRUD API
- `config.py` — 系统配置 API
- `import_export.py` — Excel 导入/导出/备份 API

### 三、SQLAlchemy ORM 模型

创建 `backend/core/models/pdca/` 目录，包含上述 8 张一期核心表的 ORM 模型。所有模型使用现有数据库连接池。

### 四、API 端点清单

| 端点 | 方法 | 说明 | 一期必做 |
|------|------|------|---------|
| `/api/pdca/records` | GET | 交易台账列表（分页/排序/筛选，支持按 code/entry_date/cycle_id 筛选） | ✅ |
| `/api/pdca/records` | POST | 新增交易记录（含数值校验） | ✅ |
| `/api/pdca/records/{id}` | PUT | 更新交易记录 | ✅ |
| `/api/pdca/records/{id}` | DELETE | 软删除交易记录（设 deleted_at） | ✅ |
| `/api/pdca/snapshots` | GET | 资金快照列表（按日期范围） | ✅ |
| `/api/pdca/snapshots` | POST | 新增/更新资金快照 | ✅ |
| `/api/pdca/snapshots/curve` | GET | 资金曲线数据（含调整后净值计算） | ✅ |
| `/api/pdca/diaries` | GET | 交易日记列表（按 record_id/cycle_id） | ✅ |
| `/api/pdca/diaries` | POST | 新增交易日记 | ✅ |
| `/api/pdca/diaries/{id}` | PUT | 更新交易日记 | ✅ |
| `/api/pdca/diaries/{id}/upload` | POST | 上传附件（jpg/png/gif ≤10MB） | ✅ |
| `/api/pdca/config` | GET | 获取系统配置 | ✅ |
| `/api/pdca/config` | PUT | 更新系统配置 | ✅ |
| `/api/pdca/export` | GET | 导出台账为 Excel（含免责声明首行） | ✅ |
| `/api/pdca/backup` | GET | 全量备份 pdca schema（pg_dump 生成 SQL） | ✅ |
| `/api/pdca/import/parse` | POST | 解析券商 Excel（华泰/中信适配器） | ✅ |
| `/api/pdca/import/confirm` | POST | 确认导入解析后的数据 | ✅ |
| `/api/pdca/stocks/search` | GET | 股票代码搜索（查询现有 stock_basic 表，自动补全） | ✅ |

**API 响应格式**：统一使用现有 `ApiResponse<T>` 信封格式 `{code, message, data}`。

### 五、关键业务规则

1. **资金曲线计算**（SRS 3.6 节）：调整后净值 = 上期净值 × (本期总资产 / (上期总资产 ± 净出入金))
2. **数值校验**：后端需二次校验 quantity>0、entry_date≤exit_date、entry_score/exit_score/trade_score 范围 0-100
3. **软删除**：trading_record 删除设 `deleted_at`，不物理删除
4. **券商导入**：解析后自动从 `stock_quotes` 补全当日 OHLC 数据
5. **导出 Excel**：首行嵌入免责声明文本

**验收方式**:
1. `SELECT count(*) FROM pdca.trading_record` 返回 0（表已创建，无数据）
2. `curl -X POST http://localhost:8000/api/pdca/records/ -H "Content-Type: application/json" -d '{...}'` 返回 200
3. `curl http://localhost:8000/api/pdca/records/?limit=10` 返回分页数据
4. `curl http://localhost:8000/api/pdca/snapshots/curve` 返回资金曲线数据
5. `curl http://localhost:8000/api/pdca/stocks/search?q=平安` 返回股票搜索建议

---

**处理记录**:
- 2026-08-10 方舟: 提单（NEW）—— PDCA 一期后端建表+数据存取接口，含 14 张表 DDL + 8 个 ORM 模型 + 18 个 API 端点。请量量认领。
- 2026-08-10 量量: 认领，开始修复（ASSIGNED）
- 2026-08-10 量量: 修复完成，待验证（VERIFY）—— 修复 `$N` 参数占位符问题（records.py/diaries.py）、股票搜索列名错误；18 个 API 端点全部通过自测验证（records CRUD/snapshots/equity-curve/diaries/config/search/import-export）
- 2026-08-10 方舟: 验证通过（VERIFY→CLOSED）—— 验收全部通过：数据库 15 张表 ✅、初始数据（1账户/8配置/3模板/2券商）✅、API 端点全部 200（config 8条/records 分页/curve 净值计算/cycles 列表/brokers 2条/stocks search 3只/创建记录 returns id）✅
