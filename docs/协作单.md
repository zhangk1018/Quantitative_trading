# 协作单

> 方舟与量量之间的工单系统，基于增强黑板模式。
> 最后更新：2026-07-28

***

## 工作流

### 状态机

```
NEW（方舟/量量提单）
  │
  ▼
ASSIGNED（量量/方舟 认领并开始处理）
  │
  ▼
VERIFY（处理完成，待对方验证）
  │
  ├─ ✅ → CLOSED（验证通过，关闭）
  │
  └─ ❌ → REOPENED（验证不通过 → 回到 ASSIGNED）
```

### 各角色职责

| 角色     | 在工单中的职责                                                 |
| ------ | ------------------------------------------------------- |
| **方舟** | 发现前端/后端问题 → 提单（NEW）；量量修复后做前端侧验证（VERIFY→CLOSED/REOPENED） |
| **量量** | 修复后端/数据问题（ASSIGNED→VERIFY）；方舟提的后端问题，量量认领修复              |
| **K**  | 仲裁争议（REOPENED 后双方无法达成一致时）；最终验收                          |

### 生命周期规则

1. **提单方**（通常是发现方）写入工单时状态为 `NEW`
2. **处理方**认领后改为 `ASSIGNED`，开始修复
3. **处理方**完成修复并验证后改为 `VERIFY`，**同时**在 topics.md 追加一行通知对方
4. **验证方**从前端/使用侧验证后：

   - 验证通过 → `CLOSED`

   - 验证不通过 → `REOPENED`，注明原因，**同时**在 topics.md 追加一行通知处理方
5. 一个工单最多 REOPENED 2 次，第 3 次需要 K 介入仲裁

***

## 通知约定

每次状态变更，**除了更新协作单本身**，同步在 [.trae/topics.md](file:///Users/zhangk/workspace/Quantitative_trading/.trae/topics.md) 追加一行：

```
[操作方→接单方 YYYY-MM-DD HH:mm] 协作单 [ID] 状态变更: OLD→NEW（摘要）
```

**关键**：必须包含"接单方"，明确下一个动作的接收方，避免读通知时不知道该谁接手。

***


## [31.0-KLINE-EXDIV-ANNOTATE-20260903] K线除权日标注（前端增强）

| 字段       | 内容                           |
| -------- | ---------------------------- |
| **状态**   | **VERIFY（后端完成，待方舟前端联调/验证）**      |
| **提单方**  | 方舟                           |
| **处理人**  | 方舟（前端）+ 量量（后端 factor_date 透出） |
| **日期**   | 2026-09-03                   |
| **影响范围** | frontend（K线详情 useStockChart）+ backend（kline_service 透出 factor_date） |
| **优先级**  | P2（增强，排 2026-09-11 周一档期）       |

**背景**：K 批示「K线除权日标注：后置，等 K 确认；协作单记录」；K 2026-09-10「请排期」。

**说明**：在 K 线顶部/图例标注除权除息日，辅助识别复权口径下的价格跳变。

**排期评估（方舟 2026-09-10 调研）**：
- 前端接入点现成：`useStockChart.ts` 已用 `lightweight-charts`，`buildMarkers()/setMarkers()` 渲染信号，除权日标注可直接复用（标记 `aboveBar` + 文本「除权」）。
- 数据层已建 `stock_adj_factor.factor_date`（V009）——港股已全覆盖（165,896 行），**A股(cn) 当前为空**。
- 后端 `kline_service.py` 已返回 `pattern_markers`；可仿照新增 factor_date 透出（读取 `stock_adj_factor` 该股 factor_date 日集合）。

**排期方案**：
- **量量（后端，本期）**：
  - **A股 factor_date 批量回填**（K 已确认纳入）：按 `detect_factor_dates`（`adj_factor` 相对变化 >1%）批量回填 `stock_adj_factor`(cn) 的 `factor_date`，仅标记除权生效日；港股已覆盖无需回填。
  - **kline_service 透出**：仿 `_query_pattern_markers` 增加除权日查询并置入 KLineResponse（新增字段 `ex_dates`）。
- **方舟（前端，本期）**：api.ts 扩展 K 线响应接收 `ex_dates` → useStockChart 走 buildMarkers 渲染「除权」标记。纯前端。

**验收方式**：港股/A股 K 线详情除权日出现「除权」标记，与复权跳变对齐。

**处理记录**:
- 2026-09-03 方舟: 提单（NEW），后置登记。
- 2026-09-10 方舟: K 确认排期，调研数据就绪度后置 ASSIGNED（方舟前端 + 量量后端透出）；识别前置依赖「A股 factor_date 为空」。
- 2026-09-10 方舟: K 确认「纳入 A 股补数」，31.0 排期定稿 → 量量工作项含「A股 factor_date 按 detect_factor_dates 批量回填」。
- 2026-09-10 量量: 后端部分完成（ASSIGNED→VERIFY）——
  1. **A股 factor_date 批量回填**：按 `detect_factor_dates` 口径（因子相对上一日变化>1%），已回填 `stock_adj_factor`(cn) **31,488 行 / 4,703 只**，耗时约 7 分钟（一次性脚本 `temp/backfill_cn_factor_date.py`）；抽样验证 000001(平安)/600519(茅台) 分红节奏正确，抽查因子变化率均 >1%。
  2. **持续维护**：`sync_adj_factor.py` 的 `expand_factor_to_daily` 新增 `factor_date` 打标（含窗口边界 seed）、`save_adj_factor` 持久化 factor_date；4 个边界场景验证通过（大除权标记/微变不标记/无seed首变更不标记/有seed窗口首日标记）。
  3. **kline_service 透出**：`KLineResponse` 新增 `ex_dates` 字段；`postgresql_storage.get_ex_dates()` 查询 factor_date；`_query_ex_dates` 入 K 线响应（A股/港股/美股三市场验证正常，NEM 类返回数据）。
  - 请方舟前端联调：`api.ts` 接收 `ex_dates` → `useStockChart` 走 `buildMarkers` 渲染「除权」标注，与复权跳变对齐。
- 2026-09-10 方舟: 前端联调完成（VERIFY→CLOSED）——
  1. **api.ts / useStockChart.ts / index.tsx** 已改：`fetchKLineData` 解析顶层 `ex_dates` 为 `exDates` 数组；`useStockChart` 新增 `buildExDateMarkers()`（`belowBar` + 淡紫 `#B39DDB` 圆点 + 文本「除权」），与信号标记合并后 `candleSeries.setMarkers()` 统一渲染；`index.tsx` 将 `klineData?.exDates` 传入钩子。
  2. **真实联调验证**（浏览器实测 600036 详情页）：K 线下方 6 月中下旬「除权」圆点标注成功渲染，位置与复权跳空缺口对齐；网络请求 `/api/kline/600036` 命中真实后端 ex_dates 数据（非 mock）。
  3. 单测已补：`tests/functions/api.test.ts` 覆盖 `ex_dates→exDates` 解析 + 无 ex_dates 兜底默认空数组；`mocks/handlers.ts` 模拟响应含 `ex_dates`。
  - 注：详情页「信号数据加载失败」告警为既有问题，与 31.0 改动无关。
  - ✅ 前端渲染验收通过 → CLOSED


---

## [34.0-BOLL-SIGNAL-FALSEPOSITIVE-20260910] K线详情页布林突破信号每日误报（后端口径）

| 字段 | 内容 |
|------|------|
| **状态** | VERIFY（根因修复完成，待方舟复核） |
| **提单方** | 方舟 |
| **日期** | 2026-09-10 |
| **影响范围** | backend |
| **优先级** | P2 |

**定位路径**: backend/core/service/signal_service.py `_generate_bollinger_signals`（约 L346-L375）；上游 K 线数据 `boll_upper` 字段来源（signal_service 内部调用 get_kline_data 读取的 K 线记录缺失 boll 字段 → 默认 0）

**问题描述**:
K 线详情页「交易信号」功能修复加载后，发现信号严重失真：600036 出现近 100 条信号，reason 大量形如 `"突破布林上轨：41.00 > 0.00"`。根因是 `_generate_bollinger_signals` 判断 `close > boll_upper` 时，`boll_upper` 恒为 0.00（其读取的 K 线数据该字段缺失/为 0）→ `close > 0` 恒真 → **每个交易日误报一条布林突破**，信号池被非真实信号堵满（最多 100 条），挤掉真实的 MACD/RSI 信号，并导致前端图表信号文本堆叠。

前端已临时缓解（同日去重 + 限量 8 条 + 短标签，见 31.0/前端修复，仅遮挡不根治），**本次为根因修复，需量量排查后端**。

**预期修复**:
1. 排查 signal_service 读取 K 线时 `boll_upper/boll_mid/boll_lower` 缺失来源：是 kline_service.get_kline_data 未回填 boll 字段，还是上游 stock_indicators 表 boll_upper 列未计算/未导出。
2. 修复数据源或判据，使 `boll_upper` 为真实布林上轨值；`boll_upper` 为 0 或无效时应跳过该信号而非用 `close>0` 误报。
3. 修复后信号量应恢复到真实数量（600036 布林突破应为少数几次而非每日一次）。

**验收方式**:
1. 浏览器/接口查询 600036 `/api/signals/600036`，reason 中不再出现 `"> 0.00"` 类误报，信号总数明显下降（数量级从 100 → 各位数/十位数内的真实信号）。
2. 抽查其他股票（如 000001、600519），布林突破信号与布林带实际关系一致（收盘价确在上轨/下轨之外才触发）。
3. 前端无需二次改动，信号自然稀疏、无需限量即清晰可读。

---

**处理记录**:
- 2026-09-10 方舟: 提单（NEW）
- 2026-09-10 量量: 根因修复完成（NEW→VERIFY）——
  1. **根因定位**：`postgresql_storage.get_kline_with_indicators` SQL 未 SELECT `boll_upper/boll_mid/boll_lower` 列 → K 线记录该字段为 None → `signal_service._generate_signals` 兜底转 0 → `_generate_bollinger_signals` 中 `close > 0` 恒真 → 每日误报 1 条布林突破。stock_indicators 表数据本身完整（600036 2026 年 167 条 boll_upper 非空、0 空值），纯查询层遗漏。
  2. **修复**：
     - storage：`get_kline_with_indicators` SQL 补选 `i.boll_upper, i.boll_mid, i.boll_lower`；
     - signal_service：`_generate_bollinger_signals` 加守卫 `if not boll_upper or not boll_lower: continue`（布林数据无效时跳过，防止上游数据缺失时 `close>0` 误报兜底）。
  3. **验证**：重启后端后实测——600036 布林突破 16 条（reason 全部真实值如 `41.69 > 41.45`，`> 0.00` 误报 **0** 条）；000001 布林 11 条、600519 布林 8 条，误报均为 0；单测脚本验证 boll=0 跳过、真实突破正常触发（2 条）。
  - 请方舟复核：`/api/signals/600036`（2026 年）无 `"> 0.00"` 误报、布林信号与布林带实际关系一致，前端信号稀疏可读后 CLOSED。
- 2026-09-10 方舟: 独立复核通过（VERIFY→CLOSED）——
  1. **代码确认**：`postgresql_storage.get_kline_with_indicators` SELECT 已含 `i.boll_upper, i.boll_mid, i.boll_lower`（L1757），`_generate_bollinger_signals` 已加 `if not boll_upper or not boll_lower: continue` 守卫。
  2. **数据源独立查证**：600036/000001/600519 2026 年各 167 条 `boll_upper` 全非空且 >0、0 值 0 条 → 佐证纯查询层遗漏、数据本身完整。
  3. **接口实证**（带认证 curl）：三只股票布林 `"> 0.00"` 误报均 **0**；布林 reason 全部真实值（如 `41.69 > 41.45`、`1308.00 > 1301.14`）。600036 布林从每日误报（≈167 条）→ **16 条**真实信号，且 MACD/RSI 真实信号保留。
  4. **前端**：详情页无「信号数据加载失败」告警，布林/除权标注正常渲染。
  - 注：600036 总信号仍约 100 条，其中约 70 条为 **RSI 超买/超卖真实信号**（非误报，属 RSI 阈值频率高），前端限流 8 条后「超买」标签仍较轻度重叠。此为独立于本工单的信号密度问题，另议优化。
  - ✅ 布林误报根因修复验收通过 → CLOSED

---

## [34.1-RSI-SIGNAL-DENSITY-20260910] RSI 信号高频（前端缓解后仍密集，转后端口径）

| 字段 | 内容 |
|------|------|
| **状态** | VERIFY（口径优化完成，待方舟复核） |
| **提单方** | 方舟 |
| **日期** | 2026-09-10 |
| **影响范围** | backend |
| **优先级** | P3 |

**定位路径**: backend/core/service/signal_service.py `_generate_rsi_signals`（RSI 超买/超卖生成逻辑与阈值判定）

**问题描述**:
34.0 布林误报修复后，/api/signals/{code} 返回的信号仍偏多：600036 总信号约 100 条，其中约 **70 条为 RSI 超买/超卖**（真实信号，非误报），占绝对多数。RSI 阈值判定（如超买 >70 / 超卖 <30）触发频率高，真实信号与震荡噪声混在一起。前端已增强去重（按信号类型分散采样，每类最多 3 条）缓解显示拥挤，但后端返回数据仍以 RSI 为主，语义上部分为震荡噪声。

**预期修复**:
评估 RSI 信号生成口径，降低触发频率/提升有效性，例如：
1. 收紧 RSI 阈值（如超买 >70 调至 >75、超卖 <30 调至 <25）；
2. 对连续超买/超卖做合并（一段持续超买只产出 1 条拐点信号）；
3. 或增加确认条件（如配合相邻两日变化，仅真正的极端值触发）。
具体口径由量量按历史信号有效性权衡，前端无需再次改动即可自然稀疏。

**验收方式**:
1. /api/signals/600036 RSI 信号数量显著下降（从 ~70 条到可辨识的正常量级）；
2. 前端详情页不做额外去重时信号也不明显堆叠；
3. 保留的 RSI 信号与价格走势（超买后回调/超卖后反弹）相关性合理。

---

**处理记录**:
- 2026-09-10 方舟: 提单（NEW）
- 2026-09-10 量量: 口径优化完成（NEW→VERIFY）——
  1. **方案**（分析 4 只股票 RSI 分布后权衡）：核心采用**连续超买/超卖段合并**——`_generate_rsi_signals` 改为状态机跟踪，仅在 RSI 从超买区回落（跌破 70）或从超卖区回升（站上 30）当天产出 1 条拐点信号，一段持续超买/超卖只对应 1 条。**保留 70/30 阈值**（收紧至 75/25 边际收益小：600036 30→26 条、600519 无收益，且改变信号语义、可能漏真实信号）。
  2. **效果**（2026 年，接口实测）：600036 RSI **98→30** 条、000001 **98→28**、600519 **88→25**；总信号降至 53-71 条。
  3. **相关性抽查**（600036 完整行情序列）：超卖回升信号（8/13、8/19）后价格反弹确认；超买回落信号（8/27、9/8）后价格回调，与走势相关性合理。
  4. **单测**：新增 `tests/test_signal_rsi_merge.py` 6 用例全过（单段合并/交替/平稳无信号/末尾未回落不产/单日不产/二次进入）。
  - 请方舟复核：`/api/signals/600036`（2026 年）RSI 信号约 30 条、信号日与走势相关性合理、前端不做额外去重也不明显堆叠后 CLOSED。

***