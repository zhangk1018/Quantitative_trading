# 量化数据获取与清洗模块依赖
pandas==2.2.0
# baostock 0.9.2 内部使用 df.append()（pandas 2.0 已移除），已 patch venv 中 resultset.py 改为 pd.concat
# 补丁位置: venv/lib/python3.11/site-packages/baostock/data/resultset.py:141
baostock>=0.9.1
APScheduler>=3.10.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
PyYAML>=6.0
# numpy>=2.0.0
numpy>=1.23.2,<2.0.0

# FastAPI 后端API依赖
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
saffier>=0.3.0

# 数据处理
scipy>=1.11.0
scikit-learn>=1.3.0
# K 线形态识别 - K 2026-06-17 决策: 直接用 TA-Lib 真库
# 系统依赖: brew install ta-lib (macOS) / apt-get install ta-lib (Linux)
# Python 包: pip install TA-Lib (C 库 0.6.4 + Python 0.6.8 已验证可装)
TA-Lib==0.6.8

# 可选依赖
python-multipart>=0.0.6
email-validator>=2.0.0

#ETL 全流程监控仪表盘
dash==2.17.0
dash-bootstrap-components==1.6.0
plotly==5.24.0
